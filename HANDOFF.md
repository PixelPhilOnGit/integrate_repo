
# Devtoolkit 交接文档

> 起初写于 2026-09-17，2026-09-18 加入 SSH 模块后重写，同日加入智能体会话模块后又补了一轮。
> 2026-09-21 加入助手模块（自研 agent 内核）后再补一轮。
> 接手前先读这一份，比读代码快。
>
> README 讲「是什么、怎么用」，这一份讲「为什么是这样、哪里会坑」。

---

## 一句话状态

**七个模块（顺序图 / Redis / 数据库 / SSH 终端 / 智能体会话 / 任务 / 助手）。**

**七个都能用了。** 助手在 2026-09-22 那一轮接上了**工具**和**对话界面**：
能真的读文件、改文件、跑命令，要动东西之前会先弹审批。见下面「助手」两节。

- SSH 是第一个**流式**模块，也是第一个带安全决策（TOFU）的模块
- 智能体会话是第一个**起用户本机进程**的模块，也是第一个**改工作区之外的文件**的模块
  （往 `~/.claude/settings.json` 和 `~/.codex/config.toml` 里装状态钩子 ——
  路径由 Rust 侧算，前端只能传枚举值）
- 助手是第一个**自己发网络请求**的模块（CSP 是 `connect-src 'self' ipc:`，
  前端根本发不出外网请求 —— 所以模型调用只能在 Rust 侧），
  也是第一个**跑我们自己的 agent 循环**的模块：上下文由我们管，所以策略才可配。
  这是它和「智能体会话」的根本区别，名字只差一个 s，别混。

凭据走**操作系统钥匙串**：连接密码 + 助手的 API key（见「凭据存储」那节）。
助手的 key 是**只写不读**的，和连接密码不一样 —— 理由见下面那一节。

**存储分两层，都落在 SQLite 上**（2026-09-20 用户拍板「这些渐渐转到 sqlite 中去吧」，
当天就一次换完）：

* **键值层**（`src-tauri/store/`，新 crate）—— 各模块原来用的 `createKeyValue`
  是「一个 JSON 文件整体读写」，现在桌面端换成 SQLite 的 `kv(key, value)` 表。
  **接口一个字没改**（`get` / `set`），所以每个模块的迁移就是换一行。
  ⚠️ **第一次启动要搬迁老文件**：一个事务里把顶层键写进 `kv` + 写标记 → 提交 →
  **最后**才把老 JSON 改名成 `.bak`（只改名不删）。读不了/JSON 坏了就**报错并且
  一个字不动**，前端退回 `tauri-plugin-store` 照常用老数据 —— 用户的连接档案
  不能因为一次升级没了。
  另一个坑：`serde_json` 的 `Value` 会**重排对象的键** ✗，搬迁本该原样搬家 ——
  所以值用 `RawValue` 存原文（测试钉着，包括反斜杠转义）。
* **结构化的表**（`src-tauri/tasks/`）—— 任务那种「一堆行、要查询」的东西直接建表。

理由和迁移规则分别写在两个 crate 的头部。

---

## 现在能做什么

### 顺序图（完整）

选本地文件夹当工作区、目录树（建子目录/重命名/**移动到别的目录**/右键菜单）、
画 UML 顺序图（六种参与者、四种消息、激活条、注释）、直接操作、撤销重做、主题、
导出 SVG / PNG / Mermaid。

**格式契约在 [`docs/seq-format.md`](docs/seq-format.md)** —— 用户会（也已经被建议）
让窗格里的 Claude Code 照着它生成 `.seq.json`。所以那份文档有一条**专门的测试**
（`tests/unit/seq-format-doc.test.ts`）把里面的例子抽出来真解析一遍：
文档和代码一漂移就红，而不是等用户照着写出一张打不开的图。

⚠️ 两个最容易被手写/AI 生成踩到的点，也写在文档里：**`x`/`y` 缺省是 0**
（会全叠在一起，排版不做自动布局）；**`from`/`to` 指向不存在的参与者时那条消息
会被静默丢掉**。

### Redis（浏览式）

侧栏「连接 → 库（带 key 数）」→ 主区 key 列表（类型标签、pattern 过滤、滚动加载）
+ 选中 key 的值（按类型换渲染）。命令台收成一个页签。`` `AUTH` `` 的回显会打码。

### 数据库（四种引擎）

连接配置里选引擎：**PostgreSQL / MySQL / ClickHouse / MongoDB**。
侧栏**第一层按引擎分**（用户要的：「新建 pg，那这个连接属于 pg」），
下面才是「连接 → 库 → 表」，点一张表生成一句查询；主区 SQL 编辑器 + 结果表格。

⚠️ **MongoDB 不是 SQL**：它只是连接的一种，工作台是**文档浏览器**
（库 → 集合 → 文档 + 一段 JSON 查询），不走 SQL 编辑器那条路。

⚠️ **Mongo 的 `find` 对不存在的集合不报错**（回一个空游标）—— 那是它的语义，
但对用户是个坑：打错集合名和「这个集合是空的」在界面上长得一模一样。所以 Rust 侧
补了一道**存在性检查**，报 `没有这个集合：xxx`，文案和浏览器版假实现保持一致
（前端假实现和 e2e 早就按这个契约来了，只有真后端漏了 —— 又是「两边各自的测试
结构性地盖不到」那个老问题）。

⚠️ **ClickHouse 一次请求只跑一条语句**（HTTP 接口的模型）。用户粘一段多条 SQL
进去会得到一条语法错误 —— **如实报错，不去拆他的语句**（拆了更难解释）。
另外它的**错误消息在中文 SQL 上只有错误码**，原因和应对见下面「踩过的坑」里
专门那一节。

**踩过的坑（真机）**：点一张 PG 的表生成的 SQL 是裸表名，执行报
`ERROR: relation "account_api" does not exist` —— PostgreSQL 解析裸表名**只看
`search_path`**，表不在 `public` 里就必然找不到。所以现在：Rust 侧列表**必须
带 `schema`**（`TableInfo` 的四个字段：schema + name + kind + rows），
生成的 SQL 一律写成 `"schema"."表名"`。**名字一律加双引号** —— 大小写混写的
表名不加引号选不中，而统一加对普通小写名毫无影响。规则和理由在
`modules/sql/core/query.ts`（纯函数，测得到）。

### SSH 终端

侧栏「连接 → 会话」，主区标签栏 + **真终端**（xterm.js）。
密码 / 私钥文件认证，**多标签**（一个连接可以同时开好几个终端，各自一条 TCP）。
切模块不关会话，回来画面还在。

**命令块**（终端左边那条色条）：每条命令一块，相邻两块交替颜色，鼠标放上去显示
「命令 + 耗时 / 有没有输出」，**单击连命令带输出复制走、双击把输出折起来**。

⚠️ 它的边界是**纯本地**推出来的（不往远端装 shell 集成）：用户按回车是精确事件，
命令原文在那一刻**从终端缓冲区里读**（Tab 补全、↑ 历史取出来的都是真实的样子）。
代价写在 `core/blocks.ts` 头部：**拿不到退出码**（所以标记用的是「耗时 / 没有输出」），
提示符的起点是近似值。折叠要把缓冲区**重放**一遍 —— 那件事的风险和护栏见
`core/byteLog.ts` 和 `hub.ts` 的 `redraw`。

**安全部分是这个模块的重心**：首次连接弹窗核对指纹（TOFU），
指纹变了**硬停**、必须手动「忘记主机密钥」才能重连。

**本地终端**：连接可以是**本机的 shell**（Windows 上是 PowerShell / cmd，
别处是 `$SHELL`），和 SSH 连接并列在同一个侧栏里 —— 标签栏、命令块、行规程
那些全部复用（反正都是终端）。Rust 那边由 `local_commands.rs` 起本地 PTY
（复用 `devtoolkit-agents` 的 pty 和 Job Object，不重写一份），
⚠️ **和智能体会话的会话表是两份**（session id 各自生成，撞了会互相顶掉）。

> 侧栏连接行右侧那个按钮是**「新开」**（再开一个终端），不是「断开」——
> 一个连接可以开好几个会话，「断开」说不清断哪一个。关会话去会话行右键。

### 智能体会话（新）

一屏跑多个 Claude Code / Codex 的**本机进程**。侧栏「工作目录 → 会话」，
主区是一棵**分屏树**（任意切、拖分隔条、关一格时兄弟顶上）。
新会话默认起一个 shell 再把命令当输入送进去（不是直接 spawn CLI —— 理由见
`core/types.ts` 的 `command` 字段）。

**一个工作目录 = 一个窗口**：每个目录有**自己的一套分屏布局**（`state.layouts`
按目录索引），点侧栏哪一条，右边就换哪一套 —— 两个项目的分屏互不干扰。
侧栏里会话行**默认收起**：一眼看到的是「我有几个窗口」，要看得点开。
窗口那一行右键有「关闭全部会话（N）」= **只收子窗口、目录留着**（有会话在跑会
先问一句）；「删除这个工作目录」才是连目录一起删。

> 为什么要有这一层：一开始是**一份全局布局**，结果用户建了四个会话之后问
> 「左侧为啥是多个窗口」—— 他要的心智是「一个项目一个窗口，窗口里才是格子」。

**侧栏顶部能按状态筛会话**（全部 / 需要你 / 在跑，带计数）。用户最早提这个需求
时的原话是「一个目录下开很多会话时只能一个个看」——而且**搜索在这儿帮不上忙**：
一个项目开十个会话之后标题全是「claude #1」「claude #2」，用户根本不知道哪个是哪个。
能用来缩小范围的只有**状态**。和顶部那个「需要你」队列不冲突：那是跨所有目录
按等待时长排的行动列表，这个是**在当前树上过滤**。

⚠️ **筛选和搜索是「与」的关系**（两个条件都得满足），和任务那边一个口径 ——
两个模块不该有两套直觉。筛选期间和搜索期间一样：树**临时撑开**、`state.expanded`
一个字不动，清空之后就回到用户自己摆的样子。这也是为什么 `filter` 和 `query`
合成了一个 `filtering` 判断，而不是各写一遍。

**工作目录能置顶**（右键「置顶」），常驻的那两三个项目排到最上面。

> ⚠️ **置顶只在渲染时排序，数组本身不重排** —— 一开始是排完写回 `state`
> 的，结果「取消置顶」回不到原位：那时候数组已经变成 `[beta, alpha]` 了，
> 稳定排序保持原序，于是 beta 赖着不走。e2e 一跑就撞出来了。
> 数组顺序永远是**用户添加的顺序**，置顶只是画的时候拎一下。

**「新建会话」是个对话框**：一次填「几个 Claude / 几个 Codex / 几个终端」，
建完**一次铺成网格**（逐格摆屏会让用户看到抖动，见 store 的 `createMany`）。
最下面是**启动参数**（全局一份，claude / codex 各一套，比如
`--dangerously-skip-permissions`），原样追加到启动命令后面，**只影响之后新建的会话**
—— 已经在跑的进程，命令行在它起来那一刻就定死了。
侧栏右键里那三个「新开 Claude Code / Codex / 终端」是「就来一个」的快捷方式，留着。

提醒是这个模块的立身之本，四处在动：**窗格边框变琥珀**、**侧栏顶部的「需要你」队列**
（等得最久的排最前，`Ctrl+Shift+U` 跳过去）、**状态栏计数**、
以及**模块图标上的角标**（用户在别的模块里时唯一看得见的地方）。

状态检测要往 Claude Code / Codex 的配置里装一个钩子，向导里能看到改了什么、
随时撤销。**状态有三个来源，可靠程度不同**，这是接这个模块前必须理解的：

| 来源 | 能给什么 | 说明 |
|---|---|---|
| Claude Code 的 hooks | 工作 / 需要你 / 完成 | 最准。**等授权要用 `PermissionRequest`**，见下 |
| Codex 的 `notify` | **只有「完成」** | **v1 用的就是这条**：只有 `agent-turn-complete` 一个事件 |
| Codex 的 hooks | 三态齐全（**下一轮**） | 0.149.0 里有一套和 Claude 对齐的 hooks 引擎（源码里叫 `ClaudeHooksEngine`），事件名一样、含 `permission_request`。**但要用户在 `/hooks` 里审阅信任一次，而且信任按哈希记账 —— 我们改一次他就要重审**。切换前必须先确认两件事：配置写哪个文件（`hooks.json` 还是 `config.toml` 的 `[hooks]`）、信任怎么落盘。这两条只能在真机上确认 |
| 终端通知序列（OSC 9 / 777） | 至少「完成」 | 零配置那条路。⚠️ 别写「Codex 默认会发」，没证实 |
| 用户在窗格里的键盘 | 「需要你」→「正在工作」 | 用户的动作，不是猜测 |
| 进程退出 | 终态 | pty 报的，比脚本可靠 |

**所以 Codex 的「需要你」在 v1 拿不到**（`notify` 只有回合完成），靠 OSC 和键盘兜底。
拿不到就是拿不到，代码里没有编一个假的「等待中」出来 —— 下一轮换 hooks 才补上。

**要命的那条**：「等授权」不能用 `Notification`。官方文档写明它要等约 6 秒、
而且**只在你看起来离开了终端时才发**；即时的信号是 **`PermissionRequest`**
（权限弹窗一出现就触发），官方自己就让你改用后者。

另外两条配置上的坑（都查过文档，不是猜的）：
- **`UserPromptSubmit` / `Stop` 不支持 matcher**（官方表格原文 no matcher support），
  给它们写 matcher 是死配置：不报错也不生效。
- **hook 一律用 exec form**（handler 里写 `args`）：不写 `args` 是 shell form，
  命令串要过 shell 分词，而 Windows 上默认 shell 是 bash（装了 Git Bash）否则
  powershell —— 一条 `C:\路径\hook.cmd waiting` 交给 bash 会很难受。
  写了 `args` 就直接 spawn、不过 shell、不做分词，`shell` 字段也被忽略，
  正好把「用户装没装 Git Bash」这个变量消掉。

**Codex 的 hooks 要用户去 `/hooks` 审阅一次**（信任按 hook 定义的**哈希**记账，
我们改一次它就得重审一次 —— 所以生成的内容要稳定，别塞时间戳）。

### 任务（新）

一件事从「待办」到「完成」的**账本**：标题 + 描述 + 状态 + 备注，就这四样。
侧栏是卡片列表（搜索 + 状态筛选 + 「还没做完的」角标），主区是一张**任务卡**
（标题、描述、备注都在卡片里改，失焦即存），检查器放状态、时间和删除。

**为什么单独一个模块**：任务是**所有事**的入口，不隶属于任何技术域 ——
挂在某个模块下的话，它的可见性就跟着那个模块走了。

**归档**（用户要的）：完成的先留在「已完成」，确认不用再看了才归档 —— 归档之后
默认不在列表里（左侧有「归档」那一档专门看），**但一条都不删**：回顾上一件事是
怎么解决的，全靠它们。状态和归档是**两件事**（做没做完 / 还要不要摆在眼前）。

⚠️ 计数（角标、筛选器上的数字）**不含归档的**，Rust 和前端一个口径 ——
否则角标会一直挂着一个数，久了就被无视。

**进度记录**（用户要的：「归档后最后回顾才能知道」）：一条任务下面挂一串
「什么时候干了什么」，每条**自动带时间戳、不给用户改**（改了就不叫记录了）。
它挂在详情卡片里，和「备注」分开：备注写的是**结论**，进度写的是**过程** ——
而过程恰恰是回顾时最值钱的、又**没法事后补**的东西。

**为什么用 SQLite**（其余模块都是「一个 JSON 整体读写」）：任务天然是**一堆行** ——
要按状态筛、按时间排、搜标题和描述，以后还要和 agent 的执行记录联表查。
JSON 也能做，但每加一个维度就得在内存里重写一遍过滤排序，而且**并发写会丢数据**
（两份整体覆盖彼此）。库文件在应用数据目录下的 `tasks.db`，结构版本记在
`PRAGMA user_version` 里，迁移**每一步都要能从任意旧版本跑上来**（用户可能从
0.3 直接跳到 0.7）。**第一次真正的迁移**是 v1 → v2（加归档列 + 进度表）：
加列用 `ALTER TABLE ... ADD COLUMN` 带默认值，**不重建表** —— 用户库里有数据，
重建一次就是一次数据搬迁的风险。这条有测试盯着（手工造一个 v1 库、塞一条数据、
再用新代码打开，断言数据一条不丢、新字段有默认值）。

⚠️ 这一轮**刻意不接 agent**（用户明确要求「先把任务弄好」）：把任务派给某个会话、
把执行结果回写，是下一步的事。外键已经开了，位置留着。

### 连接分组（新）

三个连接模块（Redis / 数据库 / SSH）的侧栏都能建**分组目录**了：把连接归归类，
一眼看出「这几台是我的生产机」。用户的原话：「连接要考虑管理什么的，最好是有目录管理」。

**只有一层，不做嵌套**（2026-09-20 确认过）—— 连接总数通常是几十条，一层 + 搜索够了；
嵌套要带出递归渲染、折叠状态、把组拖进自己子树那些边界，现在不值这个复杂度。
（真要嵌套，给 `ConnectionGroup` 加个 `parentId` 就能长出来。）

**形状和逻辑都在 `shared/connections/groups.ts`**，三个模块共用：组和连接档案存在
**同一个键值存储、不同的键**（`profiles` / `groups`）—— 形状不同，混在一个键里
迟早互相带坏。`assignGroups` 那个纯函数管「谁在哪一组、空组画不画、组没了怎么办」，
三个 `ConnectionTree` 只负责把结果画出来。

四条容易做错的：

- ⚠️ **「按引擎分」和「按分组分」是两个维度，别混**。SQL 侧栏第一层是**引擎**
  （pg/mysql/mongo/ck，**系统给的**），第二层才是**用户自己建的**分组。
  顺序是确认过的：**引擎在上、分组在下**。Redis / SSH 只有一种协议，没有引擎那层。
  分组是**全局的、不分引擎** —— 同一个「生产库」在每个引擎底下都出现（各是各的成员），
  让分组属于某个引擎的话，用户得先想「这个组是给哪个引擎的」，而他要的只是
  「把这几条放一起」。
- ⚠️ **空组必须画出来**（这条是 e2e 抓出来的）。最初的实现是「空组不画」，
  理由是「和 SQL 那边只画有连接的引擎一样」—— **那个类比是错的**：引擎是系统给的
  四个固定种类，空的是噪音；分组是**用户自己建的**，他刚点完「＋分组」，
  界面上什么都没出现，只会以为那个按钮坏了。**只有搜索时**才藏空组
  （那时用户在找具体的一条，一屏空壳是噪音）。
- **未分组的连接画在最上面**，而且不缩进 —— 新建的连接就是未分组，不放最上面的话
  用户得先展开某个组才找得着它（或者以为没建成功）。
- ⚠️ **删分组不删连接**：成员落回未分组，而且**不带确认弹窗**。用户点「删除分组」
  十有八九是想拆掉一层目录，不是想把自己填的连接全干掉 —— 所以代价只是分组没了，
  数据一条没少。这条有两个测试钉着（单测一条钉 `removeGroup`，e2e 一条钉界面），
  破了就是数据丢失级别的 bug。

搜索期间的**撑开是叠加的**（和文件树、SSH 侧栏那条规矩一样）：命中的连接如果埋在
收起来的分组里，搜索时**临时**把它撑开，`collapsed` 一个字不写 —— 写进去的话
清空搜索之后用户手动收起来的那层就再也回不去了。

### 新建连接是个弹框（新）

三个连接模块（Redis / SQL / SSH）点「新建」都弹一个对话框，填**最关键的那几个**
——名字 / 主机 / 端口 / 用户名，加上各模块特有的那一个（SQL 的引擎、Redis 的库号、
SSH 的种类）—— 确定之后才真的建。密码、认证方式、私钥路径那些留在右侧完整表单里。

用户的原话是「新建完后在右侧配置 IP、名字，能不能交互式的，新建连接弹出一个框
给用户填呢」。原来那个两段式的问题在于：连接其实**建完就能连**（默认值都填好了），
但用户想改名字和主机时得在一堆字段里找，而且很容易漏掉「它已经建好了」这件事。

**两条设计上的决定**：

1. **先收集后建，不是先建后填。** 弹框里那份草稿是一份 `newProfile()` 造出来、
   **从没进过 store** 的档案对象，确定时才 `store.createProfile(kind, draft)`。
   于是「取消」就是什么都不做（store 一个字没动、磁盘一个字没写、选中项不变）。
   ⚠️ 「先建后填、取消时删」有三个具体坏处：取消变成一次**破坏性操作**（删失败就
   留下空档案）；侧栏会闪一下、选中项跳走再跳回；切模块时 `ConnectionTree` 卸载，
   那份空档案**没人删得掉**。
2. **`createProfile` 加可选初值，签名向后兼容** —— 现有 40 多处调用（树 + 单测）
   一条都没改。⚠️ 展开时 `id`（SQL / SSH 还有 `kind`）**必须写在初值之后**：
   草稿自带一个 id（给 React 当 key 用的），漏写就静默共用同一个 id，**不报错**。
   有测试钉着，而且那条测试**验证过** —— 故意去掉那行，它确实红。

**SSH 的两个入口合并了**：原来「新建」和「本地」是两个按钮，现在是弹框里选种类。
当时留两个按钮的理由（写在旧注释里）是「点一下就是一个 SSH 连接，那个约定不该为了
一个新种类去动」—— 用户反馈之后推翻了：那两个按钮**替用户决定了种类**，
想建本地终端却点了上面那个，得到的是一个要填主机和密码的东西。

⚠️ 顺带发现一条**没做**的：`rd-btn-primary` 这个类名全仓 7 个 tsx 挂着，但
`styles.css` 里**一个字都没有定义**（按钮样式一直走 `button` 元素选择器，所以那些
「主按钮」和普通按钮长得一模一样）。加一条全局规则能把它们一起修好，但那会顺手
改掉别的模块弹框的外观 —— 这次只给 `.rd-conn-dialog` 里加了两条作用域规则。
全局那条留着，值得单独做一次。

五个侧栏都有：Redis / SQL / SSH 连接列表、智能体会话的窗口树、顺序图的文件树。
模糊匹配（`shared/search.ts`，子序列 + 打分）能搜 IP、名字、路径。

两条口径：**树形侧栏保序**（顺序就是树的形状），**平铺的连接列表按相关度排**；
搜索期间的撑开一律**叠加在点击状态之上**（`store.expanded` 一个字不动），
清空之后树回原样 —— 这条有 e2e 钉着。

### 助手（新）

图标栏最后一个（Ctrl+7）。两块：**主区域的对话** + **右侧的模型配置**。

#### 对话

说一句话，它在工作区里读文件、改文件、跑命令，过程边跑边显示。
**要动你的东西之前一定先弹审批**（改文件和跑命令都算），批了才动。

五个工具：`read_file` / `write_file` / `edit_file` / `list_dir` / `run_command`。

⚠️ **`write_file` 和 `edit_file` 是两件事**，分开是刻意的：
前者覆盖整个文件，于是「模型读了一个**被截断的**文件、顺手写回去」就会把
它没看到的那几千行**静默抹掉** —— 用户拿到的是一份看起来正常但少了内容的文件。
后者只动匹配到的那一段，匹配不到（或匹配到多处）就报错。所以工具描述里明写
「改已有文件用 `edit_file`」，读的截断提示里再说一遍。

⚠️ **`run_command` 不过 shell**（`program` + `args` 数组直接 spawn）：
管道、重定向、`&&`、通配符全用不了，要换目录就设 `cwd`。三个理由按重要性：
① 过 shell 的话**审批粒度就没了**（`argv[0]` 永远是 `sh`，「记住」于是变成
「记住一切」）；② 仓库一贯的规矩（绝不拼字符串）；③ Windows 上 shell 是 cmd /
PowerShell / Git Bash 取决于装没装 Git Bash，**同一个字符串语义不一样**。

#### 审批的粒度（安全边界的那几行）

「本会话记住」记的是 `(工具名, 归一化目标)`，不是整条命令、也不是整个工具：

| 工具 | 目标 |
|---|---|
| `write_file` / `edit_file` | **父目录**（批准「往 src/ 写」不等于批准往 `/etc` 写） |
| `run_command` | **`argv[0]` 的原文**（批准 `git` 不等于批准 `curl`） |

⚠️ **目标不做归一化。** 取基础名 / 去扩展名 / 转小写**全都是扩大授权**：
`/tmp/evil/curl` 归一成 `curl` 之后，用户之前批准的真 `curl` 会让它免审。
代价是 `git` 和 `/usr/bin/git` 要各批准一次 —— 那只是多点一次，方向是对的。

⚠️ **shell 解释器不给「记住」这个选项**（`PreparedCall.grant = None`，每次都问）。
`bash` / `cmd` / `pwsh` / `env` / `xargs` / `timeout` 这一类的语义就是
「把一段文本当命令跑」—— 记住 `bash` 等于免审之后**所有**的 `bash -c "…"`，
而用户点「记住」时看到的是**当时那一条**命令。
那张名单**不完备**（`python -c`、`make` 都能执行任意代码），也不必完备 ——
它要挡的是「顺手把最常见的那条路变成万能钥匙」，不是「禁止代码执行」。

#### 「测试连接」——配好之后能自己确认通不通

右侧面板里那个按钮：一次往返、**不走 `Channel`**，30 秒上限，
成功时会显示**模型的原话**当证据。

它只在一种情况下不可替代：**对话卡住的时候**。那时候「网络不通」和
「界面收不到事件」表现完全一样（都卡着、都不报错），走同一条路诊断
永远分不出来。这个按钮走的是另一条路 —— 它成功而对话卡住，问题就在通道那一层。

#### 模型配置

右侧「模型」面板：

- 提供方（Anthropic / OpenAI 兼容）、接口地址、模型 —— 存进 KV
- API key —— 存进系统钥匙串，**按提供方分开存**（`assistant/api_key:anthropic`）
- 没配 key 时图标栏上亮一个 `!`（角标那个槽位的注释原话就是「用户在别的模块里，
  而 agent 在等他」—— 配错了要等很久才发现是同一类问题）

三处**故意和现有模块不同**的地方：

1. **API key 只写不读。** 连接密码走 `secret_load` 会回传前端（要回填进编辑框），
   API key 不需要 —— 它只要「换一把」。少一个读接口，就少一条密钥经过 webview 的路。
2. **两家的 key 分开存。** 共用一条的话来回切会互相覆盖，症状是「我明明填过，
   怎么又要填」，而且要等到下次发请求才发现。
3. **OpenAI 兼容那一路不给默认地址。** 它没有「官方地址」可言（DeepSeek / vLLM /
   内网网关各不相同），预填一个只会让人以为请求该往那儿发。

还有一条：**「没配过」和「这台机器存不住」是两件事**（`AssistantKeyStatus` 的
`available` / `configured`）。浏览器里没有钥匙串，界面就**明说**「key 存不住」，
而不是假装存上了 —— e2e 专门验了这条。

---

## 怎么跑起来

```bash
npm install
npm run dev          # 浏览器版（不需要任何系统依赖，最容易跑起来）
npm run tauri:dev    # 桌面版（需要 Rust + 系统 WebView 依赖，见 README）
```

**浏览器版不是玩具**：headless 环境起不了原生窗口，Playwright 全靠它驱动。
所以每个连接类模块都自带一份**足够真的假实现**（假 SSH 有真的行规程、命令历史
和一个内存文件系统：`cd 项目` 之后再 `pwd` 真的会变）。

```bash
npm run typecheck    # 类型检查
npm test             # 1059 个纯逻辑单测（秒级）
npm run test:e2e     # 207 个端到端测试（真实 Chromium，这台机器上跑 6 分半）
cd src-tauri && CARGO_BUILD_JOBS=2 cargo test   # 528 条（助手那套是 185 条）
```

### ⚠️ 跑测试/编译之前必读

```bash
nice -n 19 ionice -c3 env CARGO_BUILD_JOBS=2 cargo test
```

- **这台机器只有 2 核**。cargo 会把两个核吃满，code-server 抢不到 CPU 就会掉线。
  套 `nice` 让交互式会话永远优先。
- **不要并发跑多个构建**。`CARGO_BUILD_JOBS=2` 不是可选项，默认并行度会 OOM。
- **先 `df -h`。** 磁盘满的时候，失败的样子**和磁盘毫无关系** —— MongoDB 报的是
  WiredTiger 的栈（`StorageEngineImpl::loadCatalog` 崩），docker 拉不动镜像看着像
  网络问题。这一条是真被坑掉一轮才加的（见下面「数据库测试夹具」那节）。
- 跑完测试**检查有没有残留的数据库/sshd 进程**（见「踩过的坑」里那段惨案），
  走 Docker 的那两组还要看一眼 `docker ps -a` 和 `docker volume ls`。

### Rust 测试要装服务端

```bash
sudo apt-get install -y redis-server postgresql mysql-server openssh-server
```

集成测试**自己拉起随机端口、不落盘的真实例**，`Drop` 时杀掉。没装会明确报错
并给安装命令，**不会静默跳过**。

> ⚠️ **打真 `sshd` 的那组测试要 root**（sshd 要切换用户身份），所以它**不在 CI 里**，
> 和 `devtoolkit-sql` 那组一样属于本机验证。SSH 另外那两组用**进程内的 russh
> 服务端**，零系统依赖，CI 里跑的就是它们。
>
> SQL 那套还要求测试进程能切到 `postgres` / `mysql` 系统用户 ——
> 这两个引擎拒绝以 root 运行，夹具靠 `CommandExt::uid/gid` 降权。

---

## 架构

```
src/
├── shared/        通用层
│   ├── connections/   连接类模块共用的连接层（Redis / SQL / SSH）
│   ├── platform/      文件、对话框、系统偏好、键值存储
│   └── terminal/      终端实例表（SSH 和智能体会话共用）
│                      ★ 不 import 任何模块
├── shell/         外壳：模块注册表、图标栏、状态栏、错误条
│                  ★ 不认识任何具体模块，只认 Module 接口
└── modules/
    ├── diagram/   顺序图
    ├── redis/     Redis
    ├── sql/       数据库
    ├── ssh/       SSH 终端
    ├── agents/    智能体会话（一屏多个 agent）
    ├── tasks/     任务（SQLite）
    └── devplaceholder/  占位（验证「加模块 = 一个目录 + 一行」）
```

```
src-tauri/
├── src/         commands.rs（文件）/ redis_commands.rs / sql_commands.rs /
│                ssh_commands.rs / agent_commands.rs
├── core/        devtoolkit-core：路径安全 + 文件操作
├── redis/       devtoolkit-redis：连接管理、命令执行、回复解析
├── sql/         devtoolkit-sql：MySQL / PostgreSQL 连接与查询
├── ssh/         devtoolkit-ssh：连接、认证、主机密钥、PTY 会话
├── agents/      devtoolkit-agents：本机进程、状态事件目录、集成配置读写
└── tasks/       devtoolkit-tasks：任务库（SQLite，rusqlite bundled）
```

六个 crate 都**不依赖 tauri**，所以能脱离 WebKit/GTK 跑测试（含打真服务端的集成测试）。

### 加一个模块要做什么

**写一个目录 + `src/shell/registry.ts` 加一行。**

```ts
export const MODULES = [
  diagramModule, redisModule, sqlModule, sshModule, agentsModule, tasksModule,
  devPlaceholderModule,
];
```

接口在 `src/shell/types.ts` 的 `Module`。**五个真实模块都是这么加上去的**。

> ⚠️ 注册表顺序 = 图标栏顺序 = `Ctrl+1..6` 的顺序，**是面向用户的**。
> 另外 `main.tsx` 的 `defaultExtension` 取 `MODULES[0]`，顺序图必须留在第一位。
> 加模块会让 `tests/e2e/shell.spec.ts` 里那两个「有几个模块」的断言挂掉 —— 那是**故意的**。

---

## SSH 模块：为什么和前面三个长得不一样

这是第一个**流式**模块。前三个都是「发一条请求、等一个结果」，SSH 是长连接上的
双向流：远端随时吐字节，写/resize/关闭是三条独立的命令。所以有三处结构差别：

### 1. 终端字节**不走 store**

前三个模块的 store 装「连接状态 + 结果」，更新频率是人手级别。终端是每秒几十次的
字节流，每次过 `set()` 都会让侧栏和标签栏重渲染。所以分两层：

- **store**（`state/store.ts`）：档案、`runtime`（按**档案**）、`sessions`（按**会话**）、
  信任提示、已知主机。
- **`core/terminalHub.ts`**：**拥有 xterm 实例本身**，负责字节投递。React 全程看不见字节。

注意这里有两套粒度：`sessions` 按会话 id、`runtime` 按档案 id。一个档案可以同时开
好几个会话（多标签），而侧栏那个状态点回答的是「这个档案现在连着没有」——
它是**从 sessions 推出来的**（`statusOf`），不另存一份。

### 2. 终端「藏起来」而不是「销毁重建」

切标签、切模块时，终端实例**不销毁**，容器挪到屏幕外的 holder（`position: fixed; left: -20000px`
+ 固定尺寸）。回来时挪回去。

一开始想的是「销毁 xterm、回来用缓冲的字节重放」。**那行不通**：裸字节重建不出终端的
**状态** —— `DECCKM`（vim/less 里方向键发什么序列）、bracketed paste、备用屏幕、滚动区域，
全都是会话早期设置一次的**粘性模式**，不在最近的输出里。重放的结果是全屏程序切回来就
永久花屏，而且因为重排之后尺寸没变**不会发 SIGWINCH**，vim 不会重绘。

> ⚠️ holder **不能用 `display: none`** —— 那样 `clientWidth` 变 0，xterm 量不到尺寸，
> `fit()` 会算出垃圾值（实测踩过：远端按 2 列换行）。

### 3. IPC 上多了一个 Channel

`ssh_open` 收一个 `tauri::ipc::Channel`，读循环把输出合并（8ms / 4KB）后往里推。
**四条容易踩的：**

- **一个 Channel 只能用一次。** Rust 侧丢掉 Channel 会往 JS 发 `{end: true}`，
  JS 收到就把回调**注销** —— 首次信任那一次必然在发消息之前就返回，会把通道打死。
  所以**每次尝试都新建一个 Channel**。
- **`ssh_open` 的三种结局里，主机密钥的两种走 `Ok` 不是 `Err`。**
  前端要区分「没见过」「变了」「认证失败」，而 `Err` 那条路上只有字符串
  （`shared/platform/invoke.ts` 会把非字符串 reject 变成 `String(e)`）。
- **字节走 base64**：不用 `Vec<u8>`（serde 编成数字数组，每个字节三四个字符），
  也不在前端拼字符串（SSH 的数据边界会切断多字节 UTF-8，中文会变 U+FFFD）。
- **`ssh_write` 必须串行**：每次是独立 invoke，到达顺序不保证，打字会乱序成 `sl`。
  串行化在 `services/tauri.ts` 的一条 promise 链里。

---

## 智能体会话：状态是怎么从外面进来的

### 钩子写「事件文件」，不是调我们的程序

约定是：**一个会话一个文件，状态写在文件名里**（`waiting.<会话id>`，
状态只有 `working` / `waiting` / `done`），时间戳用**文件的 mtime**。

为什么这么绕：

- **写它的必须是一段纯 shell 脚本，不能是 Devtoolkit 主程序。**
  Tauri 二进制启动要 200ms+，而 Claude Code 的钩子**同步阻塞 agent** ——
  每回合卡 200ms，用户会以为工具坏了。写个文件是 shell 一句重定向的事。
- **状态放文件名而不是内容**：shell 里生成时间戳要面对 `%TIME%` 的 locale 差异，
  写内容要面对引号和换行 —— 而文件名只需要 `echo. > "%DIR%\%1.%PANE_ID%"`。
- **mtime 当时间戳**：操作系统替我们记，脚本一个字都不用写。

好处顺带有三个：不占端口、Windows 上不弹防火墙、**应用没开着时事件也不丢**
（攒在目录里，下次启动读到；对不上号的会话丢掉）。

**防伪造两道**：目录在应用自己的数据目录下；文件名里的会话 id 必须对得上一个
活着的会话（id 是 `newId` 生成的随机串）。第二道在 store 里，因为会话表在它手里。

### OSC 扫描器是**旁观者**，一个字节都不吃

终端通知序列（`OSC 9` / `OSC 777`）是零配置那一路。⚠️ 别在文档里写「Codex 默认会发
OSC」—— 那个说法（配置键 `tui.notifications`）**没证实**：Codex 0.149.0 的二进制里
grep 不到，官方能核对的只有 `notify`。二手资料里到处都是，但别当事实。
（Claude Code 那边是有文档的：hook 可以返回 `terminalSequence` 代为发射，
白名单是 OSC 0/1/2/9/99/777 和裸 BEL，而且**在 Windows 上可用**。）

扫描器**必须只观察、不删字节**：OSC 这个命名空间里
还有设置窗口标题（0/2）、超链接（8）、终端能力上报，顺手把认出来的序列删掉会把
它们一起弄坏，而且坏法很隐蔽（标题不更新了，没人会想到是通知扫描干的）。

序列**跨字节块**是常态（`ESC` 在一块结尾、`]` 在下一块开头），所以扫描器必须是有状态的。
它按会话分开持有（两个会话的字节流混在一起会拼出谁也认不出来的东西）。

### 「我知道了」只对**这一次**等待有效

规则是「状态一变，确认作废」，**刻意不看时间戳**。原来是「ackAt 比 statusAt 新就算
已确认」，那要求两个时间戳同源 —— 而 statusAt 有一部分来自**事件文件的 mtime**，
那是外部程序写的、可能被复制过来、时钟还可能是偏的。两个时钟对不上时，要么
永远不再提醒你，要么提醒个没完。

### 事件要按时间顺序**逐条**应用

曾经写的是「同一个会话只留最新那条」（为了避开没开机时攒下的一堆）。那有个真漏洞：
`working` 紧接着 `waiting` 这一对会被压成一条 `waiting`，于是「它离开过等待又回来了」
这个事实就没了 —— 而你确认过的会话正要靠它**再次**叫醒你。状态机本身会吃掉重复的
信号（返回同一个对象），所以逐条应用既不会多记流水账，也不会丢信息。

---

## 助手：为什么它和「智能体会话」不是一回事

名字只差一个 s，做的事正相反：

| | `devtoolkit-agents` | `devtoolkit-assistant` |
|---|---|---|
| 跑的是什么 | **别人的 CLI**（claude / codex） | **我们自己的循环** |
| 上下文谁管 | 那些 CLI 自己管 | **我们管** —— 所以策略才可配 |
| 进程在哪 | 用户的 PTY 里 | 网络请求在进程内，工具跑在项目目录里 |

**「用户可选上下文策略」这个诉求在 agents 那边落不了地** —— 上下文是那些 CLI
的内部事务。这就是要自研的理由，也是这个模块存在的全部原因。

### crate 的各个文件，各管一件事

```
message.rs        provider 中立的对话模型（认不出来的块用 RawValue 逐字节留着）
sse.rs            能吃任意切分的 SSE 状态机（按字节缓冲，不按字符串）
turn.rs           把流式事件聚合成一轮（工具输入**按 index 分桶**）
context.rs        上下文组装：切原子块 → 按策略挑 → 净化
loop_runner.rs    循环的**决策**（纯函数）：拿到一轮之后该干什么
approval.rs       审批闸门：循环在这里挂起
session.rs        驱动层：三个 IO 口子全抽成 trait
transport.rs      真 HTTP（hyper + tokio-rustls）—— **全 crate 唯一碰 socket 的地方**
provider/         两个 provider 的请求构造 + 事件翻译
tool.rs           工具的**纯逻辑**：契约、schema 校验、审批判定、GrantKey 粒度
tools/            工具的**那一半碰 IO 的**：fs.rs 文件四件套 + exec.rs 跑命令
journal.rs        记账口子（同步、不返回错误 —— 记账失败不该弄挂一个 run）
transcript.rs     会话记录落盘（SQLite）：每轮的用量 + 结局
```

⚠️ `tool.rs` 和 `tools/` 的切法是刻意的：「该不该问用户」「参数合不合法」
是**最容易写错、又最不该出错**的两件事，它们必须在毫秒级的单测里覆盖全部分支 ——
所以留在一个不碰文件系统、不起进程、不认识 Tauri 的文件里。
`tools/` 那边才是 `fs::read` 和 `Command::spawn`。

### 为什么内核不留一个 `if provider == ...`

两家的差异（工具结果放哪、参数是对象还是字符串、停止判据叫什么）**全部在
`provider/` 里消化掉**，循环只看中立模型。翻译层**不做判断** ——
「截断了要不要执行工具」那类决定全在 `loop_runner` 里，翻译层多一个判断
就多一个测试盖不到的分支。

### 三层纯函数：这是它能测得动的原因

`context::assemble` / `message` 的互转 / `loop_runner::next_actions`
**都不碰网络、不碰磁盘、不碰 Tauri**。所以「`tool_use` 被切开要整对丢掉」
这种断言是**一个 `assert_eq!`**，而不是「起个假服务端、跑一圈、看日志」。

驱动层因此**没有分支** —— 分支全在纯函数里，那一层只剩搬运。

### 循环里必须写对的六条（写错不报错，只是行为诡异）

1. **每轮把完整的 `response.content` 追加回去** —— 只取 text 会丢 `tool_use` 块。
2. **同一轮的多个 `tool_result` 放在同一条 user 消息里**（拆成多条会静默地
   训练模型不再并行调用工具）。
3. **工具失败也要回 `is_error: true`**，不能丢。
4. **`stop_reason == "max_tokens"` 且带 `tool_use` 时不要执行那个工具** ——
   截断后的 JSON 常常**仍然能解析**成一个"看着合法"的部分对象，
   所以「能解析」不是放行理由。回一条错误让模型重来。
5. **`refusal` 时该轮工具一个都不执行**（拒绝可能把 `tool_use` 切一半）。
6. **流中断要丢弃整轮的半截**，不追加任何东西 —— 追加进去会造出没有配对
   `tool_result` 的 `tool_use`，下一轮直接 400。这条最阴：表现是「偶发 400、
   重试就好」。

### 上下文：两个正交的轴，不是一个平铺的四选一

用户原话是「滚动 / 滑动 / 计入列表 / 存入 Milvus」四个平级选项。但前三个在决定
**这一轮往模型里塞什么**，第四个在决定**怎么从历史里找回来** —— 而且
「计入列表」不是策略，它是另外三个的**前提**（没有全量日志，向量库没东西可索引）。

改成的形状：**落盘无条件** + 策略枚举（全量 / 滚动 / 摘要 / 检索）
+ **向量索引是独立开关**。这样选了「检索」的人不会因此丢掉原始日志。

组装时的不变量里，最要紧的两条：

* **`tool_use` 和它的 `tool_result` 必须同生同死** —— 按「原子块」切，
  不按消息条数切。切在中间，请求直接 400。
* **thinking 块要剥掉。** 官方要求 thinking 块原样回传**且历史只追加**，
  而我们的策略**恰恰在改写历史** —— 改写过之后旧的 thinking 块会被判为无效，
  新账号/新模型上直接 400，而那个 400 看不出和上下文策略有任何关系。
  **只有我们这种自己管上下文的客户端才会踩这个。**

另外「滚动」的语义定成了**「任务原文 + 最近 N 轮」**，不是「最近 N 轮」：
一个 agent 循环从头到尾**只有一条用户消息**（任务原文），
真按最近 N 轮砍等于把任务本身砍掉 —— 模型就不知道自己为什么要做这些事了。

### 审批闸门：取消**不是**拒绝

这一条我写错过一次，值得单独记：

* **拒绝** = 回一条 `is_error` 的工具结果，**循环继续跑**（模型会换个做法）
* **停止** = **整个 run 结束**

早先版本把「取消某个 run」实现成「把它挂着的审批都回答成拒绝」—— 结果是
用户点了停止、审批被拒、模型换个法子继续烧钱。**跟用户的意图正好相反。**
所以取消是**每个 run 自己的一条信号**（`cancel_pair`），不是闸门上的一个动作。

闸门本身有四条不变量，每条都有测试：

1. **先登记、再发事件** —— 反过来的话前端可能在登记之前就回答了，
   那条回答石沉大海（表现：弹层消失了但什么也没发生）。
2. **发事件时绝不持锁** —— 有个测试专门让 `emit` 回调里直接回答闸门，
   持锁调的话那个测试会**挂死**。
3. **所有路径都要摘登记**（回答 / 取消 / 超时）—— 漏了就是长 run 的内存泄漏。
4. **重复回答是 no-op**，不是「改成拒绝」（那已经晚了，还会误导用户）。

⚠️ **审批默认不超时**（`RunSpec::approval_timeout = None`）。这是刻意的：
这个应用已有的设计就是「agent 可以一直等你」（agents 那边有专门的「需要你」
状态 + 角标）。十步任务弹十次、次次自动拒，比等着更烦人。代价是挂起状态
**必须在界面上非常显眼**，并且随时可取消。

---

## ⚠️ 踩过的坑（省得你再踩）

### 最容易骗到自己的那个

- **跑 release 二进制之前先把 dev server 关掉**，并且**确保没有旧的 Xvfb 占着 `:99`**。
  release 二进制不内嵌前端的话会去连 `devUrl`（localhost:5173），
  开发机上它**永远是"好的"**，装到用户机器上就是一片白。
  ```bash
  pkill -f 'vit[e]'                       # 先关 dev server
  pkill -9 -f 'Xvf[b]'; rm -f /tmp/.X99-lock   # 清掉残留的 Xvfb（它带着 X 授权，会让新起的连不上）
  npx tauri build --no-bundle
  ```
- **没有窗口管理器时 WebKitGTK 会抑制表单控件的激活事件** —— 按钮点了没反应。
  终端和按钮全是表单控件，所以真机验证**必须先起 `openbox`**。
  另外手动起 Xvfb 要加 `-ac`（关掉 X 授权），否则分多次调用进来会拿不到 cookie。

### 三个只有真链路才抓得出来的（都归智能体会话那轮的 e2e）

这三个的**共同点**比它们各自更重要：**store 的单测全绿，因为单测里假客户端的
`write` 不吐字节、hub 是个替身**。凡是「顺序」和「跨对象协作」的问题，替身都会
把它们抹平。所以那个模块另外补了一组 `agents-fake.test.ts`（假实现走完整条链路，
不碰 DOM），分工是「e2e 管画出来没有，那一组管每一环的语义对不对」。

1. **终端在会话进 state 之后才建。** 会话一进 state，React 立刻把那一格渲染出来，
   而那一格的 effect 去 hub 里找终端时还没有 —— `attach` 遇到不存在的会话是
   **静默空操作**（它按「会话可能已经关掉了」处理）。表现：侧栏、状态、退出码全对，
   **只有画面是空的**。现在 `createSession` 先 `await hub.create`，再 `patch` 进 state。
2. **按键的信号落在了进程回话之后。** 原来是先 `await client.write(...)`、再记
   「用户敲了键」。而进程收到 Enter 往往**立刻**输出，输出里带着 OSC 通知 ——
   于是刚收到的「需要你」当场被那条 `user-typed` 改回「正在工作」，通知里那句话也没了。
   因果反了：**用户的那一下发生在先**，改成先记信号、后写进程。
3. **信息少的那条信号覆盖了信息多的那条。** OSC 说了「等待你的确认」，紧接着
   hook 那条不带说明的事件也到了：状态一样，但说明被抹成了空，界面上只剩一个
   光秃秃的「需要你」。现在状态机里区分「明确没有说明」（`null`）和
   「这条没带说明」（`undefined`），后者保留原来的说明。

### 这一轮真机验证抓到的那条（只有它能抓到）

**`#[serde(rename_all = "camelCase")]` 加在枚举上，只改变体名，不改变体内部的字段名。**

```rust
#[serde(tag = "kind", rename_all = "camelCase")]           // ❌ Key → key 生效，private_key_path 不生效
#[serde(tag = "kind", rename_all = "camelCase",
        rename_all_fields = "camelCase")]                   // ✅
```

症状是 `invalid args 'config' for command 'ssh_open': missing field 'private_key_path'`。

**为什么两边测试都没抓到**，这一点比 bug 本身重要：
- 浏览器版 e2e 走 `services/web.ts` 的假实现 —— **根本不经过 serde**；
- Rust 集成测试在 Rust 里构造 `SshConfig` —— **不经过反序列化**。

**教训**：前端和 Rust 之间那条缝，两边各自的测试**结构性地盖不到**。
现在 `ssh/src/session.rs` 底部有一组 `contract` 测试，拿**前端会发出来的那个 JSON 字面量**
去反序列化。以后加 IPC 类型时照抄——而且要手写字段名，别照着 Rust 结构体拼。

### CSS 类名先 grep 再用（又栽了一次）

`.rd-tabs button { flex: 1 }` 是给 Redis/SQL 的「浏览 / 命令台」两格切换器用的。
SSH 的标签栏图省事复用了 `rd-tabs`，结果**标签和那个 × 各占一半宽度**——
实测标签 157px 宽，label 78px、close 78px，**点标签正中间会把标签关掉**。

上一轮已经因为 `.rd-dot` 撞过文件树一次，这次换了个类接着撞。
**规矩：用任何 `rd-` 开头的类之前先 grep 一遍定义。**

### 这台机器的 shell 陷阱

- **`pkill -f '<模式>'` 会匹配到它自己的命令行**，把正在跑的那条 shell 一起杀掉。
  表现是「退出码 144、没有任何输出」，看起来像环境崩了。
  解法：让模式不自匹配 —— `pkill -f 'vit[e]'`、`pkill -f 'devtoolk[i]t'`。
  **要筛进程就把脚本写进文件**（这一轮验证 Xvfb 残留时又用到）。
- **`cmd | tail` 会把退出码吃掉**（`$?` 是管道最后一个命令的）。
  用 `set -o pipefail` 或 `${PIPESTATUS[0]}`。
- **cargo 镜像配在 `/root/.cargo/config`（无扩展名）**，它**优先于** `config.toml`。
  往 `config.toml` 写东西会被静默忽略。现成配的是阿里云稀疏索引，可用，**别动**。

### 数据库测试夹具（血泪）

- **收尾必须杀整个进程组**：数据库是 shell `&` 出来的子进程，只杀 shell 会留孤儿。
  **而且不能写 `kill -KILL -12345`** —— procps 的 kill 把它当选项解析，一个都杀不掉。
  要用 `sh -c "kill -9 -<pgid>"`。
  > 漏过两轮孤儿进程（第二次 12 个 mysqld），把机器吃到只剩 500M，
  > **用户的 code-server 会话被反复踢下线**。
- **启动失败之后不要再重试**：`OnceLock::get_or_init` 在闭包 panic 之后不缓存，
  每个后续用例都会再起一个实例 —— 形成死亡螺旋。夹具里加了原子计数器。
  > ⚠️ **加了新夹具就要照着抄这个。** ClickHouse / MongoDB 那两组（走 Docker 的）
  > 当初就漏了 —— 一次启动失败当场留下**五个**容器，因为那两条用例的 `shared()`
  > 是裸的 `get_or_init`。症状和 pg/mysql 那次一模一样，只是换了个引擎。
- **走 Docker 的那两组：`docker rm` 必须带 `-v`。** 容器是 `docker run` 出来的，
  而 mongo / clickhouse 镜像的 Dockerfile 都声明了 `VOLUME` —— **每跑一次就新建一个
  匿名卷，删容器时不会跟着删**。一个卷几百 MB，攒够十几个就把根分区吃满（真发生过：
  44 个卷 4.4GB，`/` 100%）。
  > ⚠️ **这个坑的症状极具误导性**：磁盘满之后 mongod 起不来，报的是
  > `fassertFailed` → `StorageEngineImpl::loadCatalog` 崩溃 → `Got signal: 6 (Aborted)`，
  > 一串 WiredTiger 的栈 —— **看不出一个字跟磁盘有关**。夹具又把 docker 的
  > stdout/stderr 都丢了，所以「容器起来了但没就绪」这个现象本身也不报错。
  > 卡住的时候先 `df -h` + `docker system df`，比读栈快。
- **`wait_until_serving` 里「造了个 Future 没 await」等于没等**：Mongo 那组原来写的是
  `thread::spawn(move || probe_until_serving(port))` —— 那是一句**丢弃了 Future 的表达式**，
  线程立刻结束，`start()` 根本不等容器。表现是「整体偶发地连不上」：容器起得快就侥幸
  全绿，机器一忙就成片失败。正确写法是在那个线程里现建一个 current-thread 运行时再
  `block_on`（直连的那个线程已经在 `#[tokio::test]` 的运行时里，不能直接 block）。
  > ClickHouse 那组是同步循环等 `/ping`，所以没这个问题 —— 但两组的**就绪判据**都
  > 必须「真的敲一下服务」，光看端口能连上会提前返回（docker 的转发先 accept）。
- **两个引擎都拒绝以 root 运行**，要 `CommandExt::uid/gid` 降权 + 先 chown 数据目录。
- **`mysqld --initialize-insecure` 建的 root 只认 unix socket**，直接走 TCP 会报
  `ERROR 1130`。夹具先用 socket 建一个 `root@'%'`。
- **临时目录也要 `Drop` 清理**。`core/tests/export.rs` 当初漏了，每跑一次
  `cargo test` 就往 `/tmp` 漏一批目录，攒到 32 个才发现 —— 已修。

### ClickHouse：错误正文在中文 SQL 上会丢（读 crate 源码才找得到）

**这一条是整轮里最费劲的**，值得完整记一遍 —— 因为它的每一层伪装都很像别的问题。

现象：写错一条 SQL，结果区里只显示 `Code: 62`，**没有引擎原话**（表名、出错位置全没有）。
而契约（`conn.rs` 的 `Failure`）要求「引擎拒绝一条 SQL = 一条结果」，前端假实现
也照样给了 `没有这个集合：xxx` 这种话 —— 只有真机这条路上光秃秃一个数字。

查的时候踩过的三个假线索，都排除掉了才见底：

1. ~~lz4 压缩~~：crate 默认带 `compress=1`（ClickHouse **自己那套分块格式**，不是标准
   lz4 frame）。关掉（`.with_compression(Compression::None)`）之后错误正文确实变成一段
   明文 JSON，`exception` 字段里就是原话 —— 于是先按这个改了，**结果中文 SQL 还是只有
   错误码**。所以压缩只是其中一半。
2. ~~chunked / TCP 分包~~：真机的响应把 `\r\n`、body、`0\r\n\r\n` 拆成好几个包发。
   拿假服务器逐字节复刻这个时序，**crate 照样能拿到 body**（中间还因为自己多写了一个
   CRLF 骗了自己一轮 —— 头的结束符是**两个** `\r\n` 包凑出来的，复刻时 head 里不能
   自带结尾那个）。
3. ~~`default_format=JSON` 的锅~~：它确实是错误正文变成 JSON 的原因，但去不掉
   （`fetch_bytes("JSON")` 自己会加），而且它不是根因。

**真根因**：ClickHouse 报错时会**回显用户 SQL 的片段**，而且是**按字节**截的 ——
`SELECT * FROM 不存在的表` 报出来是 `failed at position 15 ('<0xE4>')`，那个孤零零的
`0xE4` 是「不」的首字节，**单独出现不是合法 UTF-8**。而 crate 在「立即失败」这条路上
用的是严格的 `String::from_utf8`，解不出来就**把整段正文丢掉**，退回 `Code: 62`。

> 同一个 crate 的**流式**那条路（200 + 异常标签）用的是 `from_utf8_lossy`，旁边还有一行
> 注释写着「不该因此丢掉异常消息」—— 我们走的这条它没改。所以这是 crate 的不一致，
> 不是我们的用法问题。**中文 SQL 必然触发**（错误消息里回显的半个汉字），英文 SQL 没事。

**定案**：`extract_exception` 两条腿走路 —— 正文是合法 JSON 就掏 `exception`（英文 SQL
走这条，原话完整），只剩错误码就查表翻成人话（`Code: 62 —— 语法错误`）。查表只放含义
稳定的几个，查不到就只报码：**宁可少说，不要编**。测试是互补的两条
（`引擎拒绝一条_sql_时是结果不是断连` 钉中文的兜底、`纯_ascii_的_sql_出错能拿到引擎原话`
钉好路没被改坏）。

**顺带一个能省半天的手法**：定位这类「crate 到底拿到了什么」的问题，**别读源码猜，
去抓包**。一个几十行的 Python TCP 转发（双向 dump）就能把请求和响应原样打出来，
比在 crate 里加 eprintln 快得多；假服务器用来复刻单点变量（分包、编码、头部）也很灵。

### russh 的三条行为（都读过源码，写错了不报错，只是表现得「说不清哪里不对」）

1. **`Handle` 的 `Drop` 是空操作**（源码里就一句 `debug!("drop handle")`）。
   丢掉它**不断开连接** —— 远端 shell 和 PTY 会一直挂着，keepalive 还在每 30 秒发一次。
   要断必须显式 `eof()` + `close()` + `disconnect()`。
2. **`ChannelMsg::Close` 到不了客户端的接收端**（`encrypted.rs` 里处理成
   `channels.remove()`，不往接收端发消息）。所以 `ChannelReadHalf::wait()` 返回 `None`
   才是**正常的会话结束信号**。按「匹配 Eof/Close」写读循环，要么 panic，
   要么在关掉的 channel 上**空转 100% CPU**。
3. **`request_pty` 的第一个参数是 `want_reply`**。传 `false` 不报错，
   只是静默降级成**没有 PTY 的 shell** —— 没有行规程、没有作业控制、
   `Ctrl+C` 杀不掉前台进程。

另外：`ChannelWriteHalf` **不是 `Clone`**（结构体没 derive），外面要套 `Arc`；
`client::Config` 的 `keepalive_interval` 默认是 `None`（死连接永远发现不了），
要自己设；`Handler::check_server_key` 的**默认实现拒绝一切**，TOFU 正好挂在这上面。

### 智能体会话那一轮踩到的

- **关窗格要杀「两个」进程组，不是一个。** 子进程 spawn 时 `setsid()` 过，
  它是一个会话首进程、自己的组的组长；可它**开了作业控制之后**，跑在前台的
  那个命令（`claude` 本尊）会被放进**另一个**组 —— 那才是 tty 的前台组。
  只杀前者 = shell 死了 claude 还活着；只杀后者 = 反过来。两个都杀才对
  （`pty.rs` 的 `kill_tree`，写成测试钉住了：起一个会 fork 的脚本，
  断言孙进程也没了）。
  > 自己 `setsid()` 逃走的进程（`nohup`、daemon）**故意不覆盖** —— 那正是
  > `setsid` 的用途，所有终端模拟器都是这条边界。Windows 上的 Job Object
  > 反而更彻底（除非显式 `CREATE_BREAKAWAY_FROM_JOB`）。
- **`portable-pty` 的 `Child::kill()` 在 Windows 上只有一句 `TerminateProcess`**
  （源码 `win/mod.rs`，就一行），指望不上。Windows 要靠 Job Object
  （`KILL_ON_JOB_CLOSE`）—— 顺带的好处是**Devtoolkit 自己崩掉也收尸**，
  因为进程一死内核就关句柄。
- **`2>/dev/null` 拦不住重定向失败。** 那种时候说话的是 **shell 自己**，
  不是被执行的命令，所以错误照样打到用户的终端上（实测 dash：钩子脚本在事件
  目录不存在时打出一句 `cannot create ...: Directory nonexistent`）。
  得把重定向放进子 shell：`( : > "$F" ) 2>/dev/null`。
  包装脚本现在还会先 `mkdir -p` —— 用户在自己的终端里跑 claude 时，
  钩子**必须**安静，这条有测试盯着（退出码 0 + 没有输出 + 没有文件）。
- **钩子命令一律用 exec 形式**（`command` + `args`），两个平台都是。
  shell 形式（不写 `args`）那条路，`"C:\路径\hook.cmd" waiting` 只在 cmd 里成立：
  PowerShell 里带引号的路径必须加 `&` 调用运算符，Git Bash 里根本跑不了 `.cmd`
  —— 而 Claude Code 用哪个 shell 取决于**机器上有没有装 Git Bash**。
  exec 形式直接 spawn 可执行文件、不过 shell、不分词，把这个变量整个消掉
  （代价：Unix 上的 `.sh` 必须真的有 +x 和 shebang，`write_hook_script` 会补）。
  见 `integration.rs` 的 `claude_entry`。
- **⚠️ `portable-pty` 必须精确钉在 `=0.8.1`，升到 0.9 终端会白屏。**
  两个版本只差 `CreatePseudoConsole` 的 flags 一位：0.9 多一个
  `PSUEDOCONSOLE_INHERIT_CURSOR`，它会让 ConPTY 发 `ESC[6n` 问光标位置、
  **不问到就不往下产出**（MS 文档里 "cause the calling application to hang"
  说的就是这一类）。上游 wezterm#6783 从 2025-03 开到现在没修。
  副作用要记着：0.8.1 的 `ExitStatus` **没有 `signal()`**（0.9 才加），
  所以「被信号杀掉」这件事我们自己记（`PtySession` 的 `killed` 标志）。
- **`TERM` 要我们自己注入**（`xterm-256color`，Windows 再补 `COLORTERM=truecolor`）：
  桌面启动的进程环境里通常没有它，里的程序按「未知终端」处理 —— 配色没了、
  TUI 降级，而且**不报错**，只是「看起来怪怪的」。
- **Windows 上 PATH 会被 portable-pty 从注册表盖掉**（`cmdbuilder.rs` 的
  `get_base_env`）。nvm/fnm/volta/scoop 往当前会话加的路径全丢，
  表现是「在这个窗口里起不来，在我自己的终端里好好的」。spawn 前显式
  `cmd.env("PATH", std::env::var_os("PATH"))` 抢回来。
- **会话 id 要卡字符集**（`[A-Za-z0-9_-]{1,64}`）。它进到钩子脚本拼出来的文件名里
  （`> "$DIR/waiting.$ID"`），含 `/`、`\`、`..` 的 id 能让脚本**写到事件目录外面**，
  而且那条路**绕过 Rust 侧所有路径校验**。拦在注入环境变量之前。
- **测试里绝不要喂真实路径**（`%APPDATA%`、`~/.claude`）：有一组用例把
  `C:\Users\me\AppData\...` 当 data_dir 传了进去 —— 在 Linux 上那是个相对路径，
  于是 crate 目录里多出一个叫这个名字的目录，不报错、没人发现；
  同样的代码在 Windows 上会写进用户真实的 AppData。库那边现在有
  `AgentPaths::ensure_absolute` 拦着。
- **`claude doctor` 和 `codex doctor` 是现成的 schema 校验器。**
  两边都会读配置文件并把不合法的地方**逐条列出来**（`Invalid settings` /
  `could not be loaded`），而且都认 `HOME` / `CODEX_HOME` 这种临时目录 ——
  所以 `tests/claude_schema.rs` 和 `tests/codex_schema.rs` 就是「用我们自己的
  代码装一遍，再让真的 CLI 去读它」。**写错 schema 是静默失效**（配置躺着、
  界面显示已启用、状态点不动），只有这个能抓到。它们不在 CI 里（runner 上没装
  这两个 CLI），属于本机验证。
- **pty 会把我们敲进去的命令回显出来。** 测试里「等 `KID=` 出现」会先等到
  **回显**里的那个 `KID=$!`（后面跟的不是数字）—— 这类断言最容易假绿。
  `tests/common/mod.rs` 的 `read_pid` 是等「标记后面真的跟着数字」那一次。
- **重放字节会把「粘性模式」丢掉 —— 折叠那条路的全部风险就在这儿。**
  命令块折叠只能靠重放（xterm 删不掉缓冲区中间几行），而文件头部早就写明
  「裸字节重建不出终端状态」。补法是把当前模式再设一遍（`hub.ts` 的
  `stickyModes`：DECCKM、bracketed paste、插入模式、原点模式……）。
  丢了这些**不报错**，只是方向键发错序列、粘贴不带包装 —— 用户只会说
  「这个终端怪怪的」。
  > ⚠️⚠️ **那串必须写在「重放内容」的**前面**，不能垫在后面当哨兵** ——
  > 这条是用户报上来才发现的。`stickyModes` 里有 `\x1b[?6h` / `\x1b[?6l`
  > （原点模式），而按 DECOM 的规矩**切换它会把光标送回原点**。垫在最后的话：
  > 重放出来的内容明明已经把光标带到了末尾，这一下又给打回左上角 ——
  > **折叠之后光标跑到最上面，用户一打字就插在历史中间**。
  > 量出来的数字：折叠前后 `cursorLine` 从 19 掉到 0。有一条 e2e 专门钉这个
  > （`ssh.spec.ts` 里「折叠之后光标要跟着内容走」）。
  >
  > 排错路上还试岔过一次：一开始怀疑是 `term.reset()` 的时序（它是同步的，
  > 而 `write()` 是异步队列），换成走队列的 `\x1bc`（RIS）—— **没用**，
  > 光标照样回零。真凶是上面那串模式。现在两个都在：`\x1bc` 清场 +
  > 模式写在内容之前。
  另外两条护栏：**备用屏幕里不重放**（全屏程序那不是「一段段输出」）、
  **输出没停不重放**（半路插进来几行，内容和行号立刻对不上，
  而且是静默地对不上）。折叠之后行号要整体挪（`tracker.remap`）——
  重放改变的是行数，色条的位置全跟着变。
- **⚠️ 「VS Code 里 claude 好好的，窗格里跑不起来」→ 根因是 PATH 是登录时的快照。**
  用户真机上的完整经过，值得记下来（因为这类的表现极具迷惑性）：

  * 窗格里 `claude` 报「requires git-bash」，而他自己的 VS Code 终端里一切正常；
  * 他手动设 `CLAUDE_CODE_GIT_BASH_PATH` 指向 `D:\software\git\install\Git\bin\bash.exe`
    （他确认路径没问题），claude 照样说 `unable to find ... path`；
  * 关键线索是「同一个路径，他那边行、我们这边不行」——
    **两个终端里的 `claude` 根本不是同一个版本**：应用是**从桌面图标起的**，
    拿到的是**登录时的 PATH 快照**，那之后装的 npm 全局目录 / 新 claude / Git
    都不在里面；VS Code 是从开发环境起的，PATH 是新的。

  修法（`pty.rs` 的 `effective_path`）：起窗格时把 **注册表里当前的 PATH**
  （`HKCU\Environment` + 机器的 Session Manager，`REG_EXPAND_SZ` 要展开）
  和进程自己那份**合并**、去重后交给子进程。进程自己的排前面（用户可能在会话里
  有意覆盖过），注册表补充的追加在后。

  顺带做了**环境自检**（`agent_probe` + 检查器那一格）：把**应用进程自己**解析出的
  claude / git / bash / PATH 摊开给用户看。以后这类「两边不一样」的问题不用再猜 ——
  用户拿它和 VS Code 里的 `where.exe claude` 一比就知道差在哪。

  **WSL 的 bash 是这里最大的一个坑（2026-09-20 真机确认）**：用户机器上
  `where.exe bash` 给的是 `C:\Windows\System32\bash.exe` —— 那是 **WSL 的**
  启动器，**永远在 PATH 上**，而它不是 Git Bash、claude 也不认它。所以：
  **任何"去 PATH 里找 bash"的做法都会先撞上它**（我们的探测代码注释里早写了这条，
  现在自检卡上也会直接把这一条显示出来并标注）。
  同一天还确认了另一条：**`<Git>\bin\bash.exe` 是包装器，`<Git>\usr\bin\bash.exe`
  才是本体** —— 用户那个 `bin\bash.exe` 明明存在（`Test-Path` 为 True），
  claude 拿着它照样报「找不到」。候选顺序因此改成**先 `usr\bin` 再 `bin`**。

  Git Bash 本身那条路仍然是老规矩：**用户填的优先、只设验证过的路径、
  找不到不报错**（见下一条）。

### 助手那一轮踩到的（六条，都是"看起来没问题"的那种）

**① 锁文件里有，不等于它会编译 —— 我据此做过一次误判。**

我本来打算用 `reqwest` 发 HTTP，理由是「它已经在 `Cargo.lock` 里、不新增依赖」。
**错的。** 它只挂在 tauri 的

```toml
[target.'cfg(any(target_os = "android", all(target_vendor = "apple", not(target_os = "macos"))))'.dependencies.reqwest]
```

下面 —— **桌面端一次都不会编译它**。验的办法：

```bash
ls src-tauri/target/debug/deps | grep -c '^libreqwest-'   # → 0
```

真正编出来的是 `hyper` / `hyper-util` / `tokio-rustls` / `rustls` / `webpki-roots`
（mongodb 拉进来的），所以**手写传输层反而才是零新增包**。
教训：判断"某个依赖在不在"要看**编译产物**，不要看锁文件。

**② `rustls` 的加密后端要显式指定，否则首次 HTTPS 请求时 panic。**

rustls 的 provider 是按 crate feature **自动挑**的，而同时开着 `ring` 和
`aws-lc-rs` 时那个自动挑返回 `None` → 第一次真正发请求时 panic。**编译期看不出来。**

更阴的是：**这个 feature 在「单独构建这个 crate」和「整个工作区构建」时不一样**
—— mongodb（sql crate 拉的）会开 `rustls/ring`，可 `-p devtoolkit-assistant`
的依赖图里根本没有 mongodb。同一份代码两种构建方式行为不同。

所以代码里**显式** `builder_with_provider(ring)`，不信自动挑。
（顺带纠正一句我写错的注释：`default-features = false` **挡不住** aws-lc-rs ——
feature 是叠加的，实测两个后端都在编。它只保证单独构建时不去编译那个 C 依赖。）

**③ `RawValue` 和内部标签枚举天生不兼容。**

想「`#[derive(Deserialize)]` + `#[serde(tag = "type")]` + 认不出来的块逐字节留着」，
编译不过：内部标签会把整块内容缓冲成 serde 的 `Content`，而 `RawValue`
**没法从 `Content` 里取出原始字节**（报 `invalid type: newtype struct`）。

解法是手写 `Deserialize`：**先原样收下整块 → 只看一眼 `type` → 已知类型从
原始字节重新解析**。未知类型的字节从头到尾没被解析过，保真。
另外 `RawValue` 没实现 `PartialEq`，相等判断也得手写（按**原始文本**比，
键序不同的等价 JSON 会被判不等 —— 这是刻意的，但熔断那种**语义**比较
必须自己解析成 `Value` 再比）。

**④ `serde_json` 的 `preserve_order` 会让「解析成 Value 再序列化」这个归一化失效。**

我为字节保真开了 `preserve_order`，于是 `Value` 里的对象**保留插入顺序**：
`{"a":1,"b":2}` 解析再序列化**还是** `{"a":1,"b":2}` —— 拿它做「重复调用
熔断」的签名归一化等于没做，而**代码看起来完全正确**。
后果是熔断永远不触发、安静地一直烧钱。要真归一化得**递归按键名排序**。

**⑤ 测试素材不真实，等于在测一个线上不存在的形状。**

我把「每个工具调用都必须有对应的 `tool_use` 块」这条不变量收紧之后，
`loop_runner` 和 `session` 里**各自有一处测试**立刻红了 —— 因为它们的 helper
在构造「有调用、没有块」的 `Turn`。那条不变量在正常流里恒真
（块和调用在累积器里同源），但留着它把「追加的这一轮一定自洽」变成了
**可以断言的东西**。**helper 偷懒不给块，测试就在测空气。**

**⑥ 顺带一条非代码的**：`clear` 那个 bug 里，真正的原因比报的现象深一层 ——
用户看到的是浏览器版（`fakeSsh` 只发 `\x1b[2J`），而 Linux 真机上 ncurses 的
`clear` 发的是 `\E[H\E[2J\E[3J`，多出来的 `ED3` 会 **trim 回滚区**、
让命令块的行号**失序**，症状是另一个（clear 之后新命令的色条压根出不来）。
**同一个操作在两个环境下的症状不同，别只按手上那个环境的症状去修。**

### 2026-09-22 那一轮（助手接上工具和对话，七条）

**① `needs_approval` 的默认方向是反的 —— 忘填 key 等于静默免审。**

`PreparedCall::needs_approval` 原来长这样：`let key = self.grant.clone()?;` ——
返回 `Option<GrantKey>`，而 `None` 的含义是「不用问」。于是**写 / 执行类工具只要
忘了填 `grant`，它就是静默免审的**。今天没有这样的工具（测试里的假实现都填了），
所以一直没暴露 —— 但「忘记填」的后果不该是**放宽**。

顺手掰过来了（现在返回 `bool`，`None` 那条分支明确落到「要问」），并加了一条测试
专门钉这个方向。真会用到那条分支的是 `run_command` 跑 shell 解释器的时候。

**② 审批事件发在「登记之前」，而且走的是会丢的那条路。**

`session.rs` 原来在调 `approver.approve()` **之前**就发 `ApprovalNeeded` —— 而
`approval.rs` 头部把「**先登记、再发事件**」写成了不变量 1（发早了的话，前端可能
在登记完成前就回答，那条回答石沉大海，表现是「弹层消失了但什么也没发生」）。

更麻烦的是 `EventSink::send` 是 `try_send`（满了就丢 —— 那是刻意的：「事件是告知，
不该让循环为了它停下来」），而审批事件一旦丢掉，`gate.request` 会挂到天荒地老，
界面上就是「它卡住了」。**两个问题叠在一起，而且都不报错。**

改法：`ApproveGate::approve` 多收一个 `&EventSink`，**事件由实现发**，挂在
`Gate::request` 的 `emit` 回调上 —— 那个回调本来就是为「登记完成之后、不持锁」
设计的（不变量 1 和 2 共用一个位置）。

**③ `[workspace.package]` 插错了位置，把 `default-members` 吞掉了。**

**TOML 的段是持续的**：插在 `[workspace]` 中间的话，它后面的键全归
`[workspace.package]` 管。cargo 只给一句 `unused manifest key` 的**警告**，
然后 `default-members` **静默失效** —— 那正好就是那行注释自己警告过的
「在 src-tauri 下敲 `cargo test` 只跑 app crate，核心那些测试一个都不会跑」。
写这一段的当天就犯了一次，靠编译输出里那两行 warning 抓回来的。

**④ `core::write_text_file` 不建父目录。**

它只是 `ensure_parent_dir` **检查**一下、不存在就报 `NotFound`。那是顺序图目录树的
语义（路径都从树里来，中间不会缺段），而模型给的路径很可能是 `src/new/file.txt`。
⚠️ 我的工具描述里写着「父目录不存在会自动建」—— **描述和实现对不上**，
而工具描述正是模型判断该怎么用它的依据。现在在 `execute_write` 里自己
`create_dir_all`（仍然在闸门里面：路径先过 `resolve`）。

**⑤ 前端把并行工具调用的结果配错了对，注释还很有信心地解释了错的方向。**

`settleNextTool` 里我写的是「从后往前找第一条还没结果的」，注释说「结果是按顺序
回来的，从前往后找会配错」—— **正好说反了**。结果是**按请求顺序**回来的
（`run_tools` 里逐个执行、逐个发事件），所以第一个结果配第一条痕迹。

这个 bug 在界面上**完全看不出来**：两个工具都显示了结果，只是成败对调了。
是 `tests/unit/assistant-chat.test.ts` 里那条「并行调用的结果要一条一条对上去」抓的。

**⑥ 审批弹层盖住了「停止」按钮 —— 而审批默认不超时。**

弹层铺满主区域，把下面的输入框和停止按钮一起盖住了。于是用户点了「改文件」之后
只剩「允许」和「拒绝」两条路 —— 而**「我不干了」是最正当的一种意图**。
「停止」和「拒绝」是两回事（见 `approval.rs` 的 `Cancel` 文档：
拒绝只让模型换个做法，停止是整个 run 结束），所以弹层里必须有一个。

**⑦ `rd-btn` 是标记类，`is-primary` 根本没有定义。**

按仓库的规矩用之前 grep 了一遍（那条规矩是踩出来的），发现 `rd-btn` 在 CSS 里
**没有定义** —— 按钮样式走的是 `button` 元素选择器，`rd-btn` 只是标记。
更要紧的是 **`is-primary`：好几处在用，但 CSS 里一个字都没有**。
新界面里不用它，换了一个真会渲染的类。

**⑧ 一个灰着的按钮，界面上一个字都不说。**

0.9.0 发出去之后用户试了助手，反馈是「点了发送，界面没任何变化」。
真机上一看：**发送按钮是灰的** —— 它的禁用条件有好几个（没选工作目录 /
没配 key / 配置不合法 / 正在跑），而主区域的空状态**只提示了「先选一个工作目录」**。
「没配 key」那条一个字都没说，于是用户看到的就是一个点不动的按钮。

⚠️ **「点了一个按钮什么也没发生」是失败方式里信息量为零的那一种** ——
用户只能得出「它坏了」。凡是按条件禁用的按钮，都要把「为什么不能点」说出来，
而且要说**全部**的原因：只挑一条的话，用户解决了那条还是点不动，
这次的困惑会原样再来一遍。

现在空状态列一份清单（`sendBlockers`）—— 它和 `canSend()` 是**同一个判断的两面**，
加条件时两边都要改。有 e2e 盯着它「一条条消失」。

顺带，真机截图里抓到「上下文」那个标签被挤成了两行（「上下」+「文」）：
flex 子项默认**可以缩到比内容还窄**，中文标签尤其明显
（`flex: 0 0 auto` + `white-space: nowrap` 修掉）。

**⑨ 流式响应读了一半，然后永远等下去（用户报上来的）。**

用户反馈「配置了、发了你好，但一直不返回，也不给报错」。查下来：
建连、响应头、TLS 都有 30 秒超时，**唯独流式 body 那一段没有** ——

```rust
while let Some(frame) = body.frame().await {   // ← 原来是这样
```

对端只要「连上了、响应头也回了、然后既不说话也不关连接」，这个循环就会
永远等下去，而用户那边只有一个「正在跑」的界面，**一个字都不报**：
不会重试、日志里没有、重试也没用（它根本没失败）。

⚠️ 教训是**超时要按阶段配，不是配一个总时长**：模型思考慢是正常的
（拿总时长卡会把慢模型误杀成网络问题），不正常的是**一直没有新数据**。
现在读循环用的是**空闲超时**（`transport::IDLE_TIMEOUT`，90 秒两块数据之间），
而且超时**必须报出来** —— 安静地退出循环在上层看来和「对端正常关了连接」
一模一样，而那会被当成一轮**完整的**响应（少了半截内容却当成成功）✓ 更糟。

**⑩ 「卡住」时用户无从下手 —— 加一个「测试连接」。**

同一轮加的。用户卡住的时候，有两件事**表现完全一样**：网络不通、和界面收不到事件。
走同一条路去诊断的话，永远分不出来。所以那个按钮**刻意不走 `Channel`**：
一次往返、直接返回结果 —— 它成功而对话卡住，问题就在通道那一层。

它发的是**最小请求**（不带工具、只让它回一个字），30 秒上限（对话那条路宽容得多），
成功时会显示**模型的原话**当证据 —— 只显示「成功」的话，用户分不清自己
是不是被中间设备骗了（有的代理回 200 然后什么也不给）。

顺带：界面上那个「正在跑…」现在**带秒数**（超过一分钟还会指一下那个按钮）。
「转了 3 秒」和「转了 3 分钟」是完全不同的两件事，而没有秒数的话用户只能凭感觉猜
—— 猜错的方向是**继续等**，那是最贵的一种错。

**⑪ HTTP 请求没发 `Host` 头 —— 真服务端一律回 400。**

用户报的：「`请求被拒绝了（HTTP 400）：400 Bad Request: missing required Host header`」。

根因：`transport.rs` 里只给了 hyper **路径**（origin-form）：

```rust
let mut builder = hyper::Request::builder()
    .method(hyper::Method::POST)
    .uri(&uri_path);        // ← 就一个路径
```

⚠️ **hyper 1.x 的客户端不会替你补 `Host`。** 它只要求 URI 是 origin-form，
而路径里当然没有主机名 —— 所以 Host 从别处来不了。而 HTTP/1.1 **要求**每个请求带它。
现在自己加（`Endpoint::host_header()`，默认端口不写出来）。

⚠️ **为什么集成测试一直是绿的**：那个手写的假服务器**什么都不检查**，
只把收到的原文交回去。于是「请求写对没有」这件事从来没被真正验过 ——
和 HANDOFF 里 `rename_all_fields` 那次**是同一个形状**：
假东西和真东西之间那条缝，两边的测试结构性地盖不到。

现在那个假服务器**会断言 Host 存在且非空**（每条走真 socket 的测试都盖到了）。
而且这条防线是**验证过**的：故意去掉 Host 跑一次，它确实红了 ✓
—— 一条抓不到 bug 的测试是假防线，写的时候要顺手确认一次。

**⑫ 磁盘满的时候，失败的样子和磁盘毫无关系（又犯了一次）。**

提交 ⑪ 之前跑全量，有一组 `1 passed; 5 failed`，看着像集成测试自己 flaky。
`df -h` 一看：**`/` 100%**（`target/` 占 51G，其中 `debug/incremental` 5.7G、
`release` 3.9G）。

清了那两个（+ 一个 5 天前残留的 redis 容器）之后**同样的命令全绿（529 条）**。

⚠️ 这条 HANDOFF 早就写着（「数据库测试夹具」那节），还是又踩了一次 ——
因为**「先 df -h」这个动作没有变成肌肉记忆**。跑集成测试之前先看一眼磁盘，
比读任何栈都快。

### 前端

- **`noUncheckedIndexedAccess` 开着**，`arr[i]` 一律是 `T | undefined`。
- **`Omit<联合类型, K>` 会把各分支的独有字段全丢掉**，要写
  `T extends unknown ? Omit<T, K> : never`（见 `WithoutSeq`）。
- **`justify-content: space-between` 是个陷阱**：子元素个数一变，排布就变。
- **`.rd-content` / `.rd-body` / `.rd-main` 必须是"布局透明"的** ——
  `flex: 1; `**`min-width: 0;`**` min-height: 0`。
  > ⚠️⚠️ **`min-width: 0` 和 `min-height: 0` 一样要紧，别只写后者**（原来就只写了后者）。
  > flex 子项的 `min-width` 默认是 **`auto`** —— 意思是「不许比内容更窄」。
  > 于是里面有个宽东西，就能**一路把链条撑开、撑到最外层**，滚的就不是那个小窗格
  > 而是整个页面。真机上报过：数据库查出一张几十列的表，滚轮滚的是整个应用
  > （e2e 量出来那个 `.rd-sql-table-wrap` 被撑到 13906px 宽、`scrollWidth`
  > 和 `clientWidth` 一样 —— 它**自己**当然就滚不动了）。
  > 回归测试在 `sql.spec.ts`「结果表格列多时，滚的是表格自己，不是整个页面」。
- **`FitAddon` 在容器没布局时会给 `undefined` 或者个位数的尺寸**，直接拿去
  `ssh_resize` 会让远端按 2 列换行。`core/fit.ts` 的 `clampSize` 负责兜住
  （拿不准就返回 null，宁可尺寸停在旧值上）。
- **xterm 走动态 import**：模块注册表在启动路径上把所有模块都拉起来，
  静态 import 会让每个用户无论用不用 SSH 都先下载解析一遍 xterm。

### 驱动原生窗口做真机验证

`xdotool` + `openbox` + ImageMagick 的 `import`：

```bash
export DISPLAY=:99
Xvfb :99 -screen 0 1440x900x24 -ac &   # -ac 关掉 X 授权，分多次调用才连得上
openbox &                              # 不起它的话按钮点不动
WEBKIT_DISABLE_COMPOSITING_MODE=1 WEBKIT_DISABLE_DMABUF_RENDERER=1 ./src-tauri/target/release/devtoolkit &
import -window root shot.png           # 截图，然后按像素坐标点
xdotool mousemove X Y click 1
```

**应用的数据目录是 `~/.local/share/com.devtoolkit.desktop/`**（不是 `~/.config/` ——
tauri-plugin-store 用的是 `app_data_dir`）。那份 `*.json` 可以直接预埋，
省掉一堆表单操作。

---

## 设计决定（几条不那么显然的）

- **激活条锚定消息 id，不是坐标**；**`participants` 按 x 有序、`messages` 按 y 有序是强制不变量**
- **缩放改 `viewBox` 不用 CSS transform** —— 后者把矢量文字变成位图
- **网络客户端放 Rust 侧** —— CSP 的 `connect-src` 敞不开，前端直连会被拦
- **服务器/引擎报错是「一条结果」，不是「执行失败」** —— Redis 的 `-ERR unknown command`、
  SQL 的表不存在、**SSH 的远端 shell 正常退出**，都内联显示，**不弹外壳错误条**。
  四个模块都有守门测试盯着这条。
- **浏览优先，不是命令台优先**；**命令参数逐个塞进 `Cmd` / 走驱动 API，绝不拼字符串**
- **切模块不断开连接** —— 连接是廉价且用户预期跨模块存活的资源
- **SSH 会话在切模块时也不关**（`onDeactivate` 什么都不做）。
  这一条**违背** `shell/types.ts` 里那句「终端之类需要断连接的模块会用到它」的预期，
  理由很具体：切去顺序图看一眼再回来会话没了，那是坏掉的终端客户端。
  真正该收尾的时机是应用退出；webview 重载留下的孤儿由 `init()` 里的 `closeAll()` 收。
- **主机密钥的信任状态归前端持有、判定在 Rust 侧做** —— 和「连接档案归前端、
  Rust 只存活连接」是同一条分工。前端不信任任何东西，只是持久化用户的决定。
- **「忘记主机密钥」是唯一的解信任路径**。指纹变了之后界面上**没有**「就这样继续」
  的按钮 —— 那个按钮恰恰会在真正危险的时候被顺手点掉。
- **切认证方式会清掉另一边的凭据**（切到私钥就清空密码）。用户以为「我改成密钥了，
  密码应该没了吧」而实际没有，那是安全上的意外，不只是洁癖。
- **分屏按钮 = 新开一个同类型的会话**，不是「把某个已有会话放上来」。
  分屏的意图几乎总是「再开一个」；想把已有的摆到旁边，走侧栏的右键菜单。
  新会话也**替换**当前聚焦那一格，而不是把屏幕再切一刀。
- **关一格 ≠ 关会话。** 关掉的是屏幕上的位置，进程留着（侧栏里还在，点一下又上屏）。
  真要把进程收掉是另一个动作（右键「关掉这个会话」），而且它带确认的语义边界很清楚。
- **「需要你」队列只收 waiting，不收 done。** 已完成是结果，需要你才是「现在有事」——
  混在一起的话那个数字会一直亮着，然后被无视。
- **全局外观存的是「选择」不是「结果」。** 三档里的「跟随系统」如果存成解析后的
  深色/浅色，用户以后换了系统主题，应用会停在旧配色上，而且再也找不回那一档。
  同理：**明确选了档位之后，系统主题变化不该动它** —— 用户刚做的选择被系统设置
  悄悄改掉是最让人恼火的一类问题。
- **深色只认 `<html data-theme>` 一个属性，不用 `@media (prefers-color-scheme)`。**
  纯 CSS 表达不了「用户明确选了浅色但系统是深色」；两套机制并存的话，深色那套变量
  得写两遍（媒体查询一份、属性选择器一份），迟早对不上。
- **`portable-pty` 钉死在 `=0.8.1`，不要升 0.9。** 0.9 为了暴露 `signal()` 顺手打开了
  `PSUEDOCONSOLE_INHERIT_CURSOR`：那一会让 ConPTY 往输出里插 `ESC[6n` 问「光标在哪」
  并**在后台线程等应答**，不应答就卡住（MS 文档原话是 "may cause the calling
  application to hang"，上游 wezterm#6783 至今未修）。症状是**终端一片空白且不报错** ——
  对「用来盯 agent」的应用来说这是最糟的失败模式。
  更关键的是：**`signal` 是 Unix 独有的**（portable-pty 只在 `#[cfg(unix)]` 里填它，
  Windows 上永远是 `None`），而 Windows 是第一目标平台 —— 为一个只在最低优先级平台上
  存在的装饰性字段，去换主力平台上的启动风险，不划算。
  真要 `signal()` 的话正确做法是 **vendor Codex 的 Windows PTY 后端**
  （`codex-rs/utils/pty/src/win/`，MIT，约 700 行），它在**创建时**就挂 job object，
  顺带消掉我们那个 assign 竞态窗口。`pty.rs` 是唯一碰 portable-pty 的地方，就是为这天留的。
- **关一格窗格要杀两个进程组**，不是直觉里的一个：子进程 `setsid()` 之后是会话首进程，
  但它开了作业控制、跑在前台的 `claude` 在**另一个**组（tty 的前台组）里。只杀一个必留另一半。
  测试里有一条**回归用例专门证明「只杀直接子进程」确实会留孤儿**。
- **事件目录「取走即删」**（一个事件只用一次）。文件名里的状态只有三个白名单值，
  认出来才消费；认不出来的（编辑器残留、用户手扔的）**静静留着不动** ——
  动它就可能删掉别人的东西，而收益是零。
- **模块图标上的角标是个组件，不是一个数字。** 外壳只摆一个槽位，模块在自己的角标里
  订阅自己的 store（和 `StatusItems` 同一个模式）。这样外壳依然不认识任何具体模块，
  而用户在别的模块里画图时，「有 agent 停下来等你」还剩这一个地方能看见。

---

## 环境变更（这台机器）

1. **装了服务端**：`redis-server` 7.0.15、`postgresql` 16、`mysql-server` 8.0.46、
   `openssh-server`（集成测试要用）。另外 `xdotool` / `openbox` / `imagemagick`
   在真机验证时用得上。
2. **code-server 是 HTTPS**：自签证书在 `~/.config/code-server/rustdraw.crt`。
   原因是 `http://` 不是安全上下文，`navigator.clipboard` 不可用。
   > ⚠️ **密码还是 `123456`，端口公网可达**。建议换掉或只监听 127.0.0.1。
3. **cargo 镜像不用配** —— `/root/.cargo/config` 早就有阿里云镜像了。
4. 系统里跑着一个 apt 装的 `mysqld`（监听 3306）和一个系统 `sshd`（监听 22），
   不是测试起的。测试用的实例都带 `/tmp/devtoolkit-*-test-*` 标记，好区分。

---

## 智能体会话：Windows 真机验证清单

**这台开发机是 headless Linux 容器，跑不了 ConPTY。** 下面这些只能在 Windows 上打勾，
每一项都对应一个「在 Linux 上验不了」的假设：

- [ ] 起一个 **Claude Code** 会话：终端里能看到它的 TUI，光标、颜色、备用屏幕正常
- [ ] 起一个 **Codex** 会话：同上
- [ ] 窗口拉伸/分屏之后 TUI **重绘正确**（不是按旧的列宽排版）—— resize 那条路
- [ ] 连开 3~4 个会话分屏，输入法、中文、粘贴都正常
- [ ] 在一个窗格里 `Ctrl+C`，别的窗格不受影响
- [ ] **关掉一个窗格再打开任务管理器**：没有残留的 `node` / `claude` 进程
- [ ] **直接关掉 Devtoolkit 主窗口**，任务管理器里同样没有残留
- [ ] 装上集成钩子之后，新开一个 Claude Code：随便让它干一件事，
      侧栏状态点应该从「空闲」变成「正在工作」，干完变「已完成」
- [ ] 让它问你要授权（比如让它执行一条命令），应该变「需要你」并进队列
- [ ] Codex 那边**只需要「已完成」能到**（v1 走 `notify`，它只有回合完成一个事件）。
      「需要你」拿不到是**已知缺口**，别当成 bug 报 —— 补法是下一轮换 hooks
      （见上面那张表里「Codex 的 hooks」那一行）
- [ ] **hook 会不会闪出黑框**：有人报过 Windows 上 hook 进程会弹一下控制台窗口
      （claude-code #64688）。如果闪，那是个真烦人的问题 —— 记下来，看能不能
      在包装脚本那边缓解
- [ ] 集成向导点「启用」之后再点「撤销」，`.claude/settings.json` 能还原
      （改之前会备份，备份文件就在旁边）

验不过的时候先看两处：检查器里那张「状态检测」卡片（事件目录路径 + 最近收到状态的时间），
以及 `%APPDATA%\com.devtoolkit.desktop\agents-events\` 里有没有文件出现。

---

## e2e 覆盖不到的那一截（**别把这些当 bug 报**）

playwright 跑的是**浏览器版**：各模块的 `services/web.ts` 内存假实现，
**不过 Tauri、不过 serde、不过 Channel**。所以下面这几类事情 e2e 一条都测不到，
只能在原生窗口上手验 —— 它们各自对应一个「在浏览器里恒真」的假设：

| 测不到的东西 | 为什么 | 真机怎么验 |
|---|---|---|
| `tauri::ipc::Channel` 的建立与收口 | 浏览器走的是假实现，没有 Channel | 真机开一个流式模块，看内容有没有渐进出现 |
| **Channel 被提前 drop** | 同上的反面：前端回调被注销 → **界面一片空白且不报错** | 真机起来之后立刻看有没有内容 |
| 多字节 UTF-8 在块边界被切断 | 假实现一次给整段字符串，不会切断 | 真机上看中文/emoji 有没有变成 `�` |
| **系统钥匙串**（连接密码 + 助手 API key） | 浏览器里没有钥匙串，`available` 恒为 false | 见下面那张单子 |
| 助手的**审批往返**（挂起 → 前端回答 → 循环继续） | 需要真 Channel + 真命令 | 配好 key 之后跑一个写文件的请求 |
| 真 provider 的连通性与 SSE 解析 | CSP 是 `connect-src 'self' ipc:`，前端发不出外网请求 | 填真 key，发一句话 |
| 助手的**工具真的在动磁盘** | 浏览器版没有文件系统，「读文件」是假实现里的一句台词 | 让它读一个真文件、改一个真文件，自己去目录里看 |
| **`run_command` 真起进程** | 同上。而且 Windows 上还有 PATH 快照那个坑（见下） | 让它跑 `git status` 之类，看输出对不对 |

### 助手：真机验证清单

这个清单在开发机上过不了（没 key、没真文件系统，Windows 特有的坑也复现不了）：

- [ ] 填真 key → 发一句话 → **回复是渐进出现的**（不是等一大坨）。这条同时验了
      `Channel` 建对了 —— 建错的话是**一片空白且不报错**
- [ ] 让它读一个文件，内容对得上
- [ ] 让它改一个文件 → 弹层上那行写的是**解析之后的真实路径** → 批准 → 文件真的变了
- [ ] 拒绝一次 → 它换个做法继续（**不是整个停下**）
- [ ] 点「整个停掉」→ 立刻停，挂着的审批也收掉
- [ ] 让它跑 `git status` → 输出对；再跑 `bash -c "echo hi"` → 弹层里
      **没有「记住」那个按钮**（跑 shell 不给记住）
- [ ] 批准一次 `git` 并选「本会话别再问」→ 再跑 `git` 不再问；
      但跑 `curl` **仍然要问**（批准 git ≠ 批准 curl）
- [ ] 跑一条不会结束的命令（比如 `sleep 300`）→ 到点被中止
- [ ] 跑一条**交互式**命令（比如不带参数的 `cat`）→ **立刻失败**，不是挂到超时
      （stdin 接了空设备）
- [ ] 关掉应用再打开 → 应用数据目录下有 `assistant-transcript.db`，
      用 `sqlite3` 看 `runs` / `turns` 里有数据
- [ ] **Windows 上**：让它跑一个**用户后装**的工具（nvm/fnm 装的 node、cargo 之类）——
      能找到才算过。找不到的话是 PATH 快照那个坑（`ToolsConfig::path` 的文档里写了）

**钥匙串那张单子**（和连接密码同一套，助手那份一并验）：

- [ ] 填一把 API key → 去系统凭据管理器里看到 `assistant/api_key:anthropic`
- [ ] 重启应用 → 显示「已配置」
- [ ] 「换一把」→ 旧值确实被覆盖（不是并存）
- [ ] 「删除」→ 凭据管理器里那条也没了
- [ ] 切到 OpenAI 兼容 → 显示「还没配」（**两家的 key 是分开的**）
- [ ] 在**没有钥匙串**的机器上（或 Linux 无 DBus）→ 界面**明说**存不住

---

## 下一步

**这一轮（2026-09-22）做完的**：助手从「只有配置」到**能用**。

- **真 tool runner**（`assistant/src/tools/`）—— 五个工具，路径全走
  `Workspace::resolve()` 那道唯一的闸门。详见上面「助手」那节的工具和审批粒度。
- **transcript 落盘**（`assistant/src/transcript.rs`）—— SQLite + `user_version`
  迁移，每轮一次 INSERT（`panic = "abort"` 下不攒）。
- **命令层 + 对话界面**（`assistant_commands.rs` + 前端的对话流 / 审批弹层）。
- **版本号的漂移根治** —— 见下面「发版」那节。

**助手剩下的**：

1. **二期**：摘要策略（`ContextStrategy::Summarize`）+ 工具面扩到 redis/sql/ssh/tasks。
2. **三期**：LanceDB + 检索。

⚠️ 两条**已知缺口**（不是 bug，别报）：

1. **对话跨重启不延续。** 历史只在内存里（`AssistantRuntime::histories`），
   而 `transcript` 一期只记**用量**不记消息（见 `transcript.rs` 头部）——
   应用一关，这个对话就没了。要接着聊得等三期那张 `messages` 表。
   ⚠️ 顺带：那份历史**没有上限**，聊得越久越长。收窄它靠的是上下文策略
   （钉在顶栏那个下拉上）—— 那正是这个功能存在的理由，不是「反正有策略所以随便涨」。
2. **`run_command` 超时只杀得掉直接子进程。** 不过 shell（这个模块存在的
   理由之一）意味着直接子进程就是用户要跑的那个程序，所以多数情况够用；
   但 `cargo` 底下的 `rustc`、`npm` 底下的 `node` 会活下来。
   彻底解决要 Job Object（Windows）/ 进程组（Unix），那是 `agents/pty.rs`
   在做的事（它还带着 portable-pty）—— 取舍是「先有上限，再有精确的收尸」。

⚠️ **三期动手前必须先验两件事**：`protoc` 装不装得上（LanceDB 要它，这台机器上
没有、CI 也没提），以及 **embedding 从哪来** —— **Anthropic 自己不做 embedding**
（官方推荐 Voyage，那是另一家公司、另一把 key），所以默认得走本地模型，
否则只有 Anthropic key 的用户用不了检索这条策略。

---

排期里（更早）躺着这么几件事：

1. ~~**凭据存储**~~ —— **做完了**（连接密码走系统钥匙串，见下面那节）。
2. ~~**连接管理**~~ —— **做完了**：三个连接模块有分组目录 + 搜索（见「连接分组」
   那节），智能体会话那边有会话的**状态筛选**和工作目录的**置顶**（见「智能体会话」
   那节）。用户拍板的「两边都要」都在了。
   > 唯一没做的是**工作目录的分组**（只做了置顶）。当时是二选一，选了更轻的那个 ——
   > 真需要归类的话，连接分组那套（`shared/connections/groups.ts`）整套都能搬过来。
3. ~~**智能体会话支持远程会话**~~ —— **做完了**（连你自己的另一台机器，
   见下面「远端会话」那一节）。用户确认过「远程 = 连另外一台机器」，不是容器/云开发机。
   > ⚠️ **真机验证还没做**：这台开发机上没有第二台机器可以连。要验的清单在那一节末尾。
4. **Codex 换 hooks**：补上「需要你」那一态（v1 走 `notify` 拿不到，见上面那一行）。
   动手前必须先确认两件事：配置写哪个文件、信任怎么落盘 —— 只能真机确认。
5. ~~**任务接 agent**~~ —— **做完了**：任务检查器里「派给某个会话…」把任务内容送进
   那个会话，并在进度里记两笔（派出去时、会话结束时）。见下面「跨模块：那条窄接口」。
   > ⚠️ 落地时**故意没加**「agent 外键那一列」：会话 id 是运行时的，存进数据库只会
   > 让后人以为它有意义。那份「会话 → 任务」的映射只活在内存里。

### 凭据存储（已解决）

连接密码**不再存在键值表里** —— 它走**操作系统的凭据存储**：Windows 的凭据管理器、
macOS 的钥匙串、Linux 的 Secret Service。

- **Rust 侧**：`store/src/secrets.rs` 做真活儿（`keyring` 4.x，能脱离 WebKit 测），
  `src/secret_commands.rs` 只做搬运。命令是**批量**的（一个模块几十条连接，
  一条一个 IPC 往返的话切一次模块要等一串来回）。
- **前端**：`shared/platform/secrets.ts` 按环境挑实现，`profiles.ts` 的
  `createSecretProfileStore` 在「记录列表」外面套一层。**三个模块一行都没改** ——
  它们走的是同一个 `profiles.ts`，这正是当初把这层抽出来的理由。
- ⚠️ **SSH 有两个敏感字段**：密码**和私钥口令**（`passphrase`）。所以
  `secretFields` 是可声明的，SSH 那边传 `['password', 'passphrase']`。
  两个字段打包成**一个**条目。

**三条路**（都在 `secrets.ts` 里，别混）：

| 环境 | 密码存哪 |
|---|---|
| 桌面端（有钥匙串） | 系统凭据库；键值表里只剩主机/端口/用户名 |
| 桌面端（没有钥匙串：服务器、headless） | 退回老路，**但侧栏里会挂一个 ⚠** |
| 浏览器版 | 老路（localStorage）—— 那里本来就没有钥匙串，e2e 走的就是这条 |

**几条拿不准就会丢密码的规矩**（都有测试钉着，见 `tests/unit/connection-secrets.test.ts`）：

- **「没存过」和「用不了」必须分开**：前者是正常的（新连接还没填密码），
  要当 `Ok(None)`；后者说明这台机器上根本没有钥匙串。混成一个错误的话，
  上层就没法决定「是提醒用户重填，还是该退回明文那条路」。
- **迁移的顺序**：键值表里还有明文、钥匙串里没有 → 搬过去 → **成功了才**从键值表里删。
  搬不进去就**一个字都不动**（下次再试）。
- **写的时候先钥匙串、后键值表**：反过来的话，两次写之间崩了就是「两边都没有」。
- **钥匙串那一整块包在 try 里**：它抛了不该让整个连接列表打不开 ——
  **拿不到密码是遗憾，打不开是事故**。

> ⚠️ **真钥匙串的读写在这台开发机上验不了**（headless 容器没有桌面会话）：
> `secrets.rs` 里那两条端到端测试会**明确打印说明再跳过**。
> **要在 Windows / macOS 上真验一次**：建一个带密码的连接 → 去系统凭据管理器里
> 看到那条 → 重启应用密码还在 → 删掉连接时凭据也没了。

### 远端会话（新）

智能体会话的窗格能连**另外一台机器**，在那边起 agent。侧栏「工作目录」那一行
右边多了个「远端」按钮：填主机、认证、**那台机器上的目录**，之后就和本机目录
一样用（分屏、命令块、状态点全都复用）。

用户确认过「远程 = 连另外一台机器」（不是容器/云开发机），所以这条路就是
**SSH**。

#### 怎么接的：`agents/src/remote.rs` 把 ssh 翻成 pty 的形状

连接、认证、主机密钥、PTY —— `devtoolkit-ssh` 那一套**全都有**而且被真机磨过。
所以这一层只做一件事：**把它的形状翻成 `pty.rs` 那套**，让命令层和前端
不用认识两套东西。

⚠️ **`agents` 现在依赖 `ssh`** —— 两个内核第一次互相依赖。理由：它们服务的是
同一个应用的**同一件事**（在这个窗格里跑一个 agent，只是那个进程在别处），
不是两个通用库。硬拆一个中间 crate 只会多一份版本号和一层转发。

三处形状差异（都是 ssh 那边固有的，不是选的）：

| | 本地 pty | 远端 ssh | 怎么办 |
|---|---|---|---|
| 开会话 | 同步 | **async** | 命令层分两条路（`open_remote`）；硬抹平要自己起运行时，那是"在运行时里再起一个"的坑 |
| 事件流 | `spawn` **返回** Receiver | `open` **要求传入** Sender | `remote.rs` 自己建一对、把 Receiver 交出去 —— **和 pty 一个用法** |
| 事件类型 | `PtyEvent` | `TerminalEvent` | 翻一道（`to_pty_event`）—— **前端不用认识两套** |
| 打开结果 | 二态 | **四态**（主机密钥那两种不是错误） | **抹不平也不该抹**，`AgentOpenOutcome` 原样带上去 |

#### ⚠️ 状态检测在远端**弱得多**，这是这条路最大的代价

本机那套靠「往 `~/.claude/settings.json` 装钩子 + 事件文件」——**远端写不了
我们的包装脚本**（也不该悄悄往别人的机器上写）。所以远端会话只剩两条路：

* **OSC 序列**（`core/osc.ts` 那个扫描器，零配置）
* **用户的键盘**

也就是说 **Codex 的「需要你」在远端大概率拿不到**，和本地那个已知缺口一样。
⚠️ 别当成 bug 报。另外 `DEVTOOLKIT_PANE_ID` / `DEVTOOLKIT_EVENT_DIR` 那两个
环境变量**不给远端会话注入**（那是往本机事件目录写文件用的，远端机器上
既没有脚本也不该有）。

#### 信任：和 SSH 模块**是两份**

远端会话有自己的一份 `knownHosts`（存在 agents 那个 kv 的另一个键里）。
⚠️ 所以**同一台机器，先用 SSH 模块连过、再用远端会话连，会被问两次**。
v1 接受这个重复 —— 合起来要跨模块共享一份「已知主机」，那是另一件事。

规矩和 SSH 模块**完全一致**（这条不能松）：

* 首次连弹窗核对指纹（TOFU）；
* **指纹变了硬停**，界面上没有「就这样继续」；
* 解信任的唯一路径是右键「忘记这台机器的指纹」。

⚠️ 信任是**一次性**的（`acceptedOnce` 那个 Set，用完就删、不落盘）：
它是「这一次我核对过了」的授权，**不是**「这台机器以后随便连」。
落盘的话就等于「信任过一台机器之后永远接受任何指纹」—— 那正是 TOFU 要防的。

#### 密码存在哪

远端工作目录里带着那台机器的密码 / 私钥口令 —— 它们和连接档案一样
**走系统钥匙串**（`workspaces` 那份存储也套了 `withSecrets`，
声明的是 `remotePassword` / `remotePassphrase` 两个字段）。
⚠️ 那两个字段在 `AgentWorkspace` 上是**拍平**的（不是嵌套一个 `remote` 对象）：
钥匙串那一层只认顶层字段。用 `remoteOf()` 取出来当对象使。

#### 还没验的（这台机器上验不了）

**没有第二台机器可以连。** 要真机验的：

- [ ] 加一个远端目录、连上去、终端里能敲命令
- [ ] 首次连会弹指纹核对；点了「信任」之后能连上
- [ ] 连上之后再连**不会**再问（指纹记下来了）
- [ ] **指纹变了**（在那台机器上换一下 host key）：弹窗要硬停、没有「继续」按钮
- [ ] 右键「忘记这台机器的指纹」之后再连会重新问
- [ ] 跑 `claude`，状态点从「空闲」变「正在工作」
- [ ] ⚠️ 让它问你要授权 —— **这一步大概率不会变「需要你」**（见上面那条）。
      如果没变，那是**已知缺口**，别当 bug
- [ ] 关掉窗格之后，那台机器上的进程没了（`ps` 里看不到）
- [ ] 私钥认证那条路（带口令和不带口令两种）

### 跨模块：那条窄接口（`shared/agentBus.ts`）

「任务派给会话」是第一个**真正跨模块**的功能，而现有规矩是「模块之间不通过外壳
通信」。做法是加一个**中立的槽位**：接口定义在共享层（**只有类型，不 import
任何模块**），会话模块启动时把自己的能力挂上去，任务模块读它。
外壳和共享层依然不认识智能体会话 —— 和 `terminalHub` 是同一个路子。

顺带的好处：**测试能塞一个假的进去**（`attachAgentBus`），不用为了验一条派发
逻辑去搭一整套会话。

⚠️ 这个接口**刻意做得窄**：别处要的只是「有哪些会话能接活」和「把一句话送进去」，
分屏、状态机、事件目录那些它一个都用不上，也就不该看见。

---

---

## 代码规模（参考）

| | 行数 |
|---|---|
| 前端 `src/` | ~35k |
| Rust `src-tauri/`（含各内核 crate） | ~31k |
| ├ app crate（`src-tauri/src/`） | ~2.4k |
| ├ 八个内核 crate | ~29k |
| └ 其中助手（`assistant/`） | ~10k |
| 前端测试 `tests/` | ~17k |

**测试数量**（`cargo test` + `npm test` + `npx playwright test`）：

| | 条数 |
|---|---|
| Rust 内核（八个 crate 合计） | 528 |
| 前端单元（vitest） | 1059 |
| 端到端（playwright） | 207 |

⚠️ 那三组**测的不是一回事**，别只看总数：Rust 那组验纯逻辑和真 socket/真进程，
vitest 验纯 TS，playwright 跑的是**浏览器版**（各模块的 `services/web.ts` 内存
假实现）—— 所以**凡是要经 Tauri 的命令、Channel、钥匙串，e2e 一概测不到**。
见「e2e 覆盖不到的那一截」那一节。

提交历史（`git log --oneline`）按 feature 拆得比较干净，
每个提交的 message 里都写了「为什么这么做」和踩到的坑，值得一读。
