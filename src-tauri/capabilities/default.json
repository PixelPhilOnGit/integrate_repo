{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "主窗口的默认权限：核心功能 + 文件对话框 + 偏好持久化。",
  "windows": ["main"],
  "permissions": [
    "core:default",

    "dialog:default",

    "store:default"
  ]
}
